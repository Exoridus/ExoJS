dist
node_modules
coverage

# Vendored third-party asset data and its notices - copied in, not authored here.
examples/assets/**

# Generated Monaco type shim; its exact shape is pinned by a drift test.
examples/shared/assets-global.d.ts

# Astro's generated cache, and the vendored bundles plus copied example catalog
# the site serves - none of it authored here.
site/.astro/**
site/public/**

# Generated API reference; the generator owns its shape and a sync gate pins it.
site/src/content/api/**

# Guide prose. Prettier reflows Markdown and rewrites JSX inside MDX, which is a
# large edit with no formatting question behind it.
*.mdx

*.min.*
pnpm-lock.yaml
