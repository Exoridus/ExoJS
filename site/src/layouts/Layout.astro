---
import AppShell from './AppShell.astro';

interface Props {
    title?: string;
    description?: string;
}

const { title = 'ExoJS', description = 'ExoJS docs' } = Astro.props;
---

<AppShell title={title} description={description}>
    <slot />
</AppShell>
