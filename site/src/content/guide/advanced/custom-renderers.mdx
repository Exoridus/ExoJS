---
title: '7.1 Custom renderers'
description: 'Extend rendering with custom passes and backend-specific logic.'
part: 7
chapter: 1
examples:
  - custom-renderers/custom-render-pass
  - custom-renderers/custom-triangle-renderer
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 7.1 Custom renderers

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. Custom renderers gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="custom-render-pass"></a>
<ExamplePreview chapter="custom-renderers" slug="custom-render-pass" />
Custom Render Pass isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="custom-triangle-renderer"></a>
<ExamplePreview chapter="custom-renderers" slug="custom-triangle-renderer" />
Custom Triangle Renderer isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

