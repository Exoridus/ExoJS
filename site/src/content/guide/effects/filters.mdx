---
title: '6.1 Filters'
description: 'Stack shader and color effects to control final image style.'
part: 6
chapter: 1
examples:
  - filters/blur-filter
  - filters/chromatic-aberration
  - filters/color-filter
  - filters/crt-scanlines
  - filters/custom-fragment-shader
  - filters/filter-stack
  - filters/metaballs
  - filters/noise-vignette
  - filters/palette-cycling
  - showcase/color-grading
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 6.1 Filters

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. Filters gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="blur-filter"></a>
<ExamplePreview chapter="filters" slug="blur-filter" />
Blur Filter isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="chromatic-aberration"></a>
<ExamplePreview chapter="filters" slug="chromatic-aberration" />
Chromatic Aberration isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="color-filter"></a>
<ExamplePreview chapter="filters" slug="color-filter" />
Color Filter isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="crt-scanlines"></a>
<ExamplePreview chapter="filters" slug="crt-scanlines" />
Crt Scanlines isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="custom-fragment-shader"></a>
<ExamplePreview chapter="filters" slug="custom-fragment-shader" />
Custom Fragment Shader isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="filter-stack"></a>
<ExamplePreview chapter="filters" slug="filter-stack" />
Filter Stack isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="metaballs"></a>
<ExamplePreview chapter="filters" slug="metaballs" />
Metaballs isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="noise-vignette"></a>
<ExamplePreview chapter="filters" slug="noise-vignette" />
Noise Vignette isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="palette-cycling"></a>
<ExamplePreview chapter="filters" slug="palette-cycling" />
Palette Cycling isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="color-grading"></a>
<ExamplePreview chapter="showcase" slug="color-grading" />
Color Grading isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

