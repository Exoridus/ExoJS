---
title: '4.2 Mouse and pointer'
description: 'Work with pointer events across mouse and touch-compatible devices.'
part: 4
chapter: 2
examples:
  - input/mouse-and-pointer
  - input/multitouch
  - input/pointer-to-world
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 4.2 Mouse and pointer

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. Mouse and pointer gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="mouse-and-pointer"></a>
<ExamplePreview chapter="input" slug="mouse-and-pointer" />
Mouse And Pointer isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="multitouch"></a>
<ExamplePreview chapter="input" slug="multitouch" />
Multitouch isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="pointer-to-world"></a>
<ExamplePreview chapter="input" slug="pointer-to-world" />
Pointer To World isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

