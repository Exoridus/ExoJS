---
title: '4.4 Action mapping'
description: 'Map multiple devices to the same intent-driven input actions.'
part: 4
chapter: 4
examples:
  - input/action-mapping
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 4.4 Action mapping

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. Action mapping gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="action-mapping"></a>
<ExamplePreview chapter="input" slug="action-mapping" />
Action Mapping isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

