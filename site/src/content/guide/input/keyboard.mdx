---
title: '4.1 Keyboard'
description: 'Capture keys and support configurable bindings.'
part: 4
chapter: 1
examples:
  - input/keyboard
  - input/key-rebinding
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 4.1 Keyboard

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. Keyboard gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="keyboard"></a>
<ExamplePreview chapter="input" slug="keyboard" />
Keyboard isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="key-rebinding"></a>
<ExamplePreview chapter="input" slug="key-rebinding" />
Key Rebinding isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

