---
title: '3.1 Graphics'
description: 'Draw procedural shapes and mesh-based geometry.'
part: 3
chapter: 1
examples:
  - geometry-graphics/graphics-primitives
  - geometry-graphics/infinite-grid
  - geometry-graphics/mesh-triangle
  - geometry-graphics/mesh-textured-quad
  - geometry-graphics/mesh-deformed-grid
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 3.1 Graphics

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. Graphics gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="graphics-primitives"></a>
<ExamplePreview chapter="geometry-graphics" slug="graphics-primitives" />
Graphics Primitives isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="infinite-grid"></a>
<ExamplePreview chapter="geometry-graphics" slug="infinite-grid" />
Infinite Grid isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="mesh-triangle"></a>
<ExamplePreview chapter="geometry-graphics" slug="mesh-triangle" />
Mesh Triangle isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="mesh-textured-quad"></a>
<ExamplePreview chapter="geometry-graphics" slug="mesh-textured-quad" />
Mesh Textured Quad isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="mesh-deformed-grid"></a>
<ExamplePreview chapter="geometry-graphics" slug="mesh-deformed-grid" />
Mesh Deformed Grid isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

