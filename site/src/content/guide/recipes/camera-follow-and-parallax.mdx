---
title: '8.2 Camera follow & parallax'
description: 'Create depth and motion parallax from camera and pointer movement.'
part: 8
chapter: 2
examples:
  - showcase/mouse-parallax
  - scene-graph/parallax-starfield
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 8.2 Camera follow & parallax

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. Camera follow & parallax gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="mouse-parallax"></a>
<ExamplePreview chapter="showcase" slug="mouse-parallax" />
Mouse Parallax isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="parallax-starfield"></a>
<ExamplePreview chapter="scene-graph" slug="parallax-starfield" />
Parallax Starfield isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

