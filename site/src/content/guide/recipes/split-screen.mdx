---
title: '8.4 Split screen'
description: 'Render multiple viewpoints in a shared scene timeline.'
part: 8
chapter: 4
examples:
  - application-scenes/multi-view-split-screen
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 8.4 Split screen

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. Split screen gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="multi-view-split-screen"></a>
<ExamplePreview chapter="application-scenes" slug="multi-view-split-screen" />
Multi View Split Screen isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

