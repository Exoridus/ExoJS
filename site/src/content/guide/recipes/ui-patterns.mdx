---
title: '8.7 UI patterns'
description: 'Build reusable dialog and text-flow UI behavior in-scene.'
part: 8
chapter: 7
examples:
  - showcase/dialog-system
  - showcase/typewriter-text
---

import ExamplePreview from '../../../components/ExamplePreview.astro';

# 8.7 UI patterns

When this part of a project becomes important, you need a pattern that stays predictable while your scene grows. UI patterns gives you that baseline so you can avoid one-off fixes later. We'll keep the focus on concrete runtime behavior and connect each idea to a runnable preview. Use these examples as reference slices, then open them in the playground when you want to adjust values and test variations in context.

## Examples

<a id="dialog-system"></a>
<ExamplePreview chapter="showcase" slug="dialog-system" />
Dialog System isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

<a id="typewriter-text"></a>
<ExamplePreview chapter="showcase" slug="typewriter-text" />
Typewriter Text isolates one practical pattern you can copy into your own scene and adapt with minimal wiring.

{/* TODO: prose */}

