---
title: "Time"
description: "Time-duration value object stored internally in milliseconds. Provides unit-converted accessors (`seconds`, `minutes`, `hours`) and arithmetic helpers (Time.add, Time.subtract, Time.addTime). Constants on the class hold canonical durations: Time.zero, Time.oneSecond, Time.oneMinute, Time.oneHour, plus the TimeInterval multipliers used by the `factor` parameter. `Time.temp` is a shared scratch instance for hot paths — do not store the reference across calls."
symbol: "Time"
kind: "class"
subsystem: "core"
importPath: "@codexo/exojs"
memberCount: 34
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/core/Time.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/core/Time.ts"
---
## Import

`import { Time } from '@codexo/exojs'`

Time-duration value object stored internally in milliseconds. Provides
unit-converted accessors (`seconds`, `minutes`, `hours`) and arithmetic
helpers (Time.add, Time.subtract, Time.addTime).

Constants on the class hold canonical durations: Time.zero,
Time.oneSecond, Time.oneMinute, Time.oneHour, plus
the TimeInterval multipliers used by the `factor` parameter.

`Time.temp` is a shared scratch instance for hot paths — do not store the
reference across calls.

## Constructors

- `new(time: number, factor: TimeInterval): Time`

## Methods

- `add(value: number, factor: TimeInterval): this` - Add value * factor milliseconds in place. Mutates this instance.
- `addTime(time: Time): this` - Add another Time value in place. Mutates this instance.
- `clone(): this`
- `copy(time: Time): this`
- `destroy(): void`
- `equals(__namedParameters: Partial<Time>): boolean`
- `greaterThan(time: Time): boolean`
- `lessThan(time: Time): boolean`
- `set(time: number, factor: TimeInterval): this` - Replace the stored duration with time * factor milliseconds. Pair with Time.seconds, Time.minutes, or Time.hours as the factor to construct from non-millisecond units.
- `setHours(hours: number): this`
- `setMilliseconds(milliseconds: number): this`
- `setMinutes(minutes: number): this`
- `setSeconds(seconds: number): this`
- `subtract(value: number, factor: TimeInterval): this` - Subtract value * factor milliseconds in place. Mutates this instance.
- `subtractTime(time: Time): this` - Subtract another Time value in place. Mutates this instance.
- `fromHours(value: number): Time` - Construct a Time from an hour count.
- `fromMilliseconds(value: number): Time` - Construct a Time from a millisecond count.
- `fromMinutes(value: number): Time` - Construct a Time from a minute count.
- `fromSeconds(value: number): Time` - Construct a Time from a second count.
- `now(): Time` - Current high-resolution monotonic time (performance.now()) as a Time.

## Properties

- `hours: TimeInterval`
- `milliseconds: TimeInterval`
- `minutes: TimeInterval`
- `oneHour: Time`
- `oneMillisecond: Time`
- `oneMinute: Time`
- `oneSecond: Time`
- `seconds: TimeInterval`
- `zero: Time`
- `hours: number`
- `milliseconds: number`
- `minutes: number`
- `seconds: number`

## Source

[src/core/Time.ts](https://github.com/Exoridus/ExoJS/blob/main/src/core/Time.ts)
