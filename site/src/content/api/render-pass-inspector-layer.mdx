---
title: "RenderPassInspectorLayer"
description: "Debug layer that lists every RenderNode with an active filter chain each frame. Renders a compact text panel with per-drawable rows showing the filter sequence, bounding-box dimensions, and mask/cache status. Use during development to answer: - \"Is my filter actually attached?\" → it appears in the list - \"Why does my frame have N render passes?\" → see total pass count - \"Is this drawable being re-rendered or cached?\" → `[cached]` flag For deep per-pass inspection (intermediate render-target contents, GLSL/WGSL source, uniform values), use Spector.js or Chrome DevTools' WebGPU panel — the engine emits debug-group labels around filter and mesh-custom-shader passes so those tools show meaningful pass names."
symbol: "RenderPassInspectorLayer"
kind: "class"
subsystem: "debug"
importPath: "@codexo/exojs/debug"
memberCount: 12
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/debug/RenderPassInspectorLayer.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/debug/RenderPassInspectorLayer.ts"
---
## Import

`import { RenderPassInspectorLayer } from '@codexo/exojs/debug'`

Debug layer that lists every RenderNode with an active filter chain
each frame. Renders a compact text panel with per-drawable rows showing
the filter sequence, bounding-box dimensions, and mask/cache status.

Use during development to answer:

  - "Is my filter actually attached?" → it appears in the list
  - "Why does my frame have N render passes?" → see total pass count
  - "Is this drawable being re-rendered or cached?" → `[cached]` flag

For deep per-pass inspection (intermediate render-target contents, GLSL/WGSL
source, uniform values), use Spector.js or Chrome DevTools' WebGPU panel —
the engine emits debug-group labels around filter and mesh-custom-shader
passes so those tools show meaningful pass names.

## Constructors

- `new(app: Application): RenderPassInspectorLayer`

## Methods

- `destroy(): void` - Release any scene-graph nodes or resources owned by this layer. Called by DebugOverlay.destroy. The base implementation is a no-op; subclasses override to tear down their Container subtrees.
- `pipelineRows(): RenderPipelineRow[]` - Depth-tagged rows for the inspected pipeline (empty if none is set).
- `render(backend: RenderBackend): void` - Submit the panel's Container subtree to the backend for drawing.
- `setPipeline(pipeline: RenderPipeline | null): this` - Set a RenderPipeline to list beneath the filter chains, or null to clear it.
- `update(_delta: Time): void` - Walk the scene graph and collect filter-chain entries for every RenderNode with at least one filter. Replaces the previous frame's snapshot. The current snapshot is exposed via entries for tests and external consumers.
- `describePipeline(pipeline: RenderPipeline): RenderPipelineRow[]` - Flatten a RenderPipeline into depth-tagged rows, recursing into nested pipelines. Pure and app-independent: identity is the pass object and label is display-only — no names are required.

## Properties

- `visible: boolean`
- `entries: readonly RenderPassInspectorEntry[]` - Read-only snapshot of the entries collected on the most recent update call. Useful for tests and for external tooling that wants to render its own visualisation rather than use the built-in panel.
- `pipeline: RenderPipeline | null` - The pipeline currently being inspected, or null.
- `totalPasses: number` - Total pass count across all collected entries.
- `viewMode: DebugLayerViewMode` - Whether this layer renders in the scene's world-space view or in a screen-space (canvas-pixel) view. Defaults to 'screen'. Subclasses override to return 'world' when rendering at actual scene positions.

## Source

[src/debug/RenderPassInspectorLayer.ts](https://github.com/Exoridus/ExoJS/blob/main/src/debug/RenderPassInspectorLayer.ts)
