---
title: "GamepadDefinition"
description: "A rule that matches one or more physical gamepads and produces a GamepadMapping. `ids` is an optional allow-list of vendor IDs (`\"045e\"`) or `vendorId:productId` pairs (`\"045e:028e\"`); when omitted the definition matches every device. `resolve` is called with the parsed GamepadDescriptor and must return a GamepadDefinitionResult — `null`/`undefined` to skip, a mapping otherwise."
symbol: "GamepadDefinition"
subsystem: "input"
importPath: "@codexo/exojs"
sourcePath: "src/input/GamepadDefinitions.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/input/GamepadDefinitions.ts#L58"
---
# GamepadDefinition
`import { GamepadDefinition } from '@codexo/exojs'`
A rule that matches one or more physical gamepads and produces a GamepadMapping.

`ids` is an optional allow-list of vendor IDs (`"045e"`) or `vendorId:productId`
pairs (`"045e:028e"`); when omitted the definition matches every device.
`resolve` is called with the parsed GamepadDescriptor and must return a
GamepadDefinitionResult — `null`/`undefined` to skip, a mapping otherwise.
## Properties

- `ids?: string | string[]`
- `name?: string`
- `resolve: object`
Source: [src/input/GamepadDefinitions.ts](https://github.com/Exoridus/ExoJS/blob/main/src/input/GamepadDefinitions.ts#L58)
