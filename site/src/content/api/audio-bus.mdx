---
title: "AudioBus"
description: "Hierarchical mixer node in the engine's audio routing graph. Each bus owns three Web Audio nodes (input gain, optional effect chain, stereo pan, output gain) and routes its output into its parent's input — the root bus connects to the destination. The three engine-built-in busses are constructed by AudioManager: `master` (root), `music` (child of master), `sound` (child of master). User code creates additional busses via `new AudioBus(name, { parent })` and registers them via AudioManager.registerBus. Volume is in 0..2 (1 = unity), pan is -1..1, mute is a boolean override. AudioBus.fadeIn / AudioBus.fadeOut produce smooth ramps over the output gain. Effect changes via AudioBus.addEffect / AudioBus.removeEffect rebuild the chain in place. Setup is deferred until the global `AudioContext` is unlocked (browser autoplay policy); operations that need live nodes are no-ops until that happens."
symbol: "AudioBus"
kind: "class"
subsystem: "audio"
importPath: "@codexo/exojs"
memberCount: 15
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/audio/AudioBus.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/audio/AudioBus.ts"
---
## Import

`import { AudioBus } from '@codexo/exojs'`

Hierarchical mixer node in the engine's audio routing graph. Each bus
owns three Web Audio nodes (input gain, optional effect chain, stereo
pan, output gain) and routes its output into its parent's input — the
root bus connects to the destination.

The three engine-built-in busses are constructed by AudioManager:
`master` (root), `music` (child of master), `sound` (child of master).
User code creates additional busses via `new AudioBus(name, \{ parent \})`
and registers them via AudioManager.registerBus.

Volume is in 0..2 (1 = unity), pan is -1..1, mute is a boolean override.
AudioBus.fadeIn / AudioBus.fadeOut produce smooth ramps
over the output gain. Effect changes via AudioBus.addEffect /
AudioBus.removeEffect rebuild the chain in place.

Setup is deferred until the global `AudioContext` is unlocked
(browser autoplay policy); operations that need live nodes are no-ops
until that happens.

## Constructors

- `new(name: string, options: AudioBusOptions): AudioBus`

## Methods

- `_getInputNode(): GainNode | null` - Internal: returns the input GainNode where children should connect.
- `_getOutputNode(): GainNode | null` - Internal: returns the output GainNode that connects upstream.
- `addEffect(effect: AudioEffect): this` - Append a effect to the end of the chain (before the pan stage). The chain is rebuilt in place; existing audio routes through the new effect on the next frame.
- `destroy(): void`
- `fadeIn(durationMs: number): this` - Linearly ramp the output gain from 0 to the current volume over durationMs. Cancels any in-flight ramps on the same gain node.
- `fadeOut(durationMs: number, options: object): this` - Linearly ramp the output gain to 0 over durationMs. By default mutes the bus once the ramp completes (stopAfter: true); pass stopAfter: false to let the ramp finish silently while leaving muted unchanged.
- `onceSetup(callback: object): void` - Subscribe to the moment this bus's audio nodes are ready. Internal use.
- `removeEffect(effect: AudioEffect): this` - Remove effect from the chain. No-op if not present. Caller still owns + must destroy() it.

## Properties

- `name: string`
- `inputNode: GainNode | null`
- `muted: boolean`
- `pan: number`
- `parent: AudioBus | null`
- `volume: number`

## Source

[src/audio/AudioBus.ts](https://github.com/Exoridus/ExoJS/blob/main/src/audio/AudioBus.ts)
