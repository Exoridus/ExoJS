---
title: "Tooltip"
description: "Hover tooltip attached to a RenderNode. Shows a small text label near the pointer after a short delay when the pointer enters `target`, and hides it immediately on pointer-out. The tooltip node is parented to the nearest UIRoot ancestor of `target`, so it always renders in screen space above other content. The target must have `interactive = true` for the hover signals to fire."
symbol: "Tooltip"
kind: "class"
subsystem: "core"
importPath: "@codexo/exojs"
memberCount: 2
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Source"]
sourcePath: "src/ui/Tooltip.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/ui/Tooltip.ts"
---
## Import

`import { Tooltip } from '@codexo/exojs'`

Hover tooltip attached to a RenderNode. Shows a small text label
near the pointer after a short delay when the pointer enters `target`, and
hides it immediately on pointer-out.

The tooltip node is parented to the nearest UIRoot ancestor of
`target`, so it always renders in screen space above other content.

The target must have `interactive = true` for the hover signals to fire.

## Constructors

- `new(target: RenderNode, options: TooltipOptions): Tooltip`

## Methods

- `destroy(): void`

## Source

[src/ui/Tooltip.ts](https://github.com/Exoridus/ExoJS/blob/main/src/ui/Tooltip.ts)
