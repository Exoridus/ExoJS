---
title: "ReverbFilter"
description: "Convolution reverb using a procedurally-generated impulse response loaded into a `ConvolverNode`. The impulse response is a stereo noise burst whose amplitude decays exponentially, simulating room reflections. Adjusting `durationSeconds` or `decay` rebuilds the impulse response immediately, so avoid animating these properties at audio rate — use `wet` for real-time mix control instead."
symbol: "ReverbFilter"
kind: "class"
subsystem: "audio"
importPath: "@codexo/exojs"
memberCount: 8
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/audio/filters/ReverbFilter.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/audio/filters/ReverbFilter.ts#L27"
---
## Import

`import { ReverbFilter } from '@codexo/exojs'`

Convolution reverb using a procedurally-generated impulse response loaded
into a `ConvolverNode`. The impulse response is a stereo noise burst whose
amplitude decays exponentially, simulating room reflections. Adjusting
`durationSeconds` or `decay` rebuilds the impulse response immediately, so
avoid animating these properties at audio rate — use `wet` for real-time
mix control instead.

## Constructors

- `new(options: ReverbFilterOptions): ReverbFilter`

## Methods

- `destroy(): void`

## Properties

- `decay: number`
- `durationSeconds: number`
- `inputNode: AudioNode`
- `outputNode: AudioNode`
- `ready: Promise<void>`
- `wet: number`

## Source

[src/audio/filters/ReverbFilter.ts](https://github.com/Exoridus/ExoJS/blob/main/src/audio/filters/ReverbFilter.ts#L27)
