---
title: "MeshGeometryData"
description: "Raw mesh data produced by geometry builders. `vertices` is a flat `Float32Array` of `(x, y)` pairs, `indices` is a triangle list for indexed rendering, and `points` is the original outline/perimeter path as a flat number array."
symbol: "MeshGeometryData"
subsystem: "math"
importPath: "@codexo/exojs"
sourcePath: "src/math/geometry.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/math/geometry.ts#L10"
---
# MeshGeometryData
`import { MeshGeometryData } from '@codexo/exojs'`
Raw mesh data produced by geometry builders. `vertices` is a flat `Float32Array`
of `(x, y)` pairs, `indices` is a triangle list for indexed rendering, and
`points` is the original outline/perimeter path as a flat number array.
## Properties

- `indices: Uint16Array<>`
- `points: number[]`
- `vertices: Float32Array<>`
Source: [src/math/geometry.ts](https://github.com/Exoridus/ExoJS/blob/main/src/math/geometry.ts#L10)
