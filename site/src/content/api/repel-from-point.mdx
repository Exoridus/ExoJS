---
title: "RepelFromPoint"
description: "Pushes every live particle away from a fixed point in the system's local coordinate space. Acceleration magnitude is `strength` (units / s²) along the direction `(particle − point)`. The optional `radius` controls the influence range — particles farther than `radius` are unaffected. Setting `radius = 0` (default) means infinite range with no falloff. Use cases: shockwaves, explosion blast, mouse-cursor repel field. GPU-eligible."
symbol: "RepelFromPoint"
kind: "class"
subsystem: "particles"
importPath: "@codexo/exojs-particles"
memberCount: 10
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "packages/exojs-particles/src/modules/RepelFromPoint.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/modules/RepelFromPoint.ts"
---
## Import

`import { RepelFromPoint } from '@codexo/exojs-particles'`

Pushes every live particle away from a fixed point in the system's local
coordinate space. Acceleration magnitude is `strength` (units / s²)
along the direction `(particle − point)`. The optional `radius` controls
the influence range — particles farther than `radius` are unaffected.
Setting `radius = 0` (default) means infinite range with no falloff.

Use cases: shockwaves, explosion blast, mouse-cursor repel field.

GPU-eligible.

## Constructors

- `new(x: number, y: number, strength: number, radius: number): RepelFromPoint`

## Methods

- `apply(system: ParticleSystem, dt: number): void`
- `destroy(): void` - Optional cleanup hook called from ParticleSystem.destroy.
- `uploadTextures(device: GPUDevice, textures: ReadonlyMap<string, GPUTexture>): void` - Upload texture data (Curve/ColorGradient lookup tables) to the GPU at compile time. Receives the GPUDevice and a map of texture bindings keyed by the name in WgslContribution.textures. Called once after pipeline creation. Required when wgsl declares textures.
- `wgsl(): WgslContribution` - Override to declare a GPU contribution. Returning a WgslContribution makes this module GPU-eligible; omitting (or returning undefined) forces CPU mode for any system that uses this module.
- `writeUniforms(view: DataView, offset: number): void` - Write this module's current uniform values into the shared uniform buffer at byteOffset. Layout must match the field declarations returned by wgsl (in the same order). Receives the current frame dt (seconds). Modules tracking accumulated time (e.g. noise/turbulence) should advance their internal counter here to stay in sync when running in GPU mode (where apply is not called). Required when wgsl declares uniforms. Called every frame by the system before dispatching compute.

## Properties

- `radius: number`
- `strength: number`
- `x: number`
- `y: number`

## Source

[packages/exojs-particles/src/modules/RepelFromPoint.ts](https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/modules/RepelFromPoint.ts)
