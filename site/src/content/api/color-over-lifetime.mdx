---
title: "ColorOverLifetime"
description: "Per-frame, per-particle color sampler. Each live particle's tint is set to the gradient evaluated at the particle's current `elapsed / lifetime` ratio, packed RGBA. Replaces the per-particle blend of `ColorAffector` (legacy) with a multi-keyframe gradient. GPU-eligible: the gradient is uploaded once as a 256-tap 1D RGBA8 texture and sampled with linear filtering on the GPU."
symbol: "ColorOverLifetime"
subsystem: "particles"
importPath: "@codexo/exojs"
sourcePath: "src/particles/modules/ColorOverLifetime.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/particles/modules/ColorOverLifetime.ts#L21"
---
# ColorOverLifetime
`import { ColorOverLifetime } from '@codexo/exojs'`
Per-frame, per-particle color sampler. Each live particle's tint is set
to the gradient evaluated at the particle's current `elapsed / lifetime`
ratio, packed RGBA. Replaces the per-particle blend of `ColorAffector`
(legacy) with a multi-keyframe gradient.

GPU-eligible: the gradient is uploaded once as a 256-tap 1D RGBA8 texture
and sampled with linear filtering on the GPU.
## Constructors

- `new(gradient: Gradient<>): ColorOverLifetime<>`

## Methods

- `apply(system: ParticleSystem<>, _dt: number): void`
- `destroy(): void`
- `uploadTextures(device: GPUDevice, textures: ReadonlyMap<string, GPUTexture>): void`
- `wgsl(): WgslContribution`
- `writeUniforms(view: DataView<>, byteOffset: number, dt: number): void`

## Properties

- `gradient: Gradient<>`
Source: [src/particles/modules/ColorOverLifetime.ts](https://github.com/Exoridus/ExoJS/blob/main/src/particles/modules/ColorOverLifetime.ts#L21)
