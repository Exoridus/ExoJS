---
title: "DynamicGlyphAtlas"
description: "A shared atlas that rasterizes glyphs on demand into an offscreen canvas and wraps it as a Texture for use by the Mesh-based Text renderer. Glyphs are always rasterized in white so that runtime tinting via `Mesh.tint` applies the fill color without requiring re-rasterization. Use `getDefaultGlyphAtlas()` from `atlas-singleton.ts` rather than constructing directly."
symbol: "DynamicGlyphAtlas"
subsystem: "rendering"
importPath: "@codexo/exojs"
sourcePath: "src/rendering/text/DynamicGlyphAtlas.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/text/DynamicGlyphAtlas.ts#L68"
---
# DynamicGlyphAtlas
`import { DynamicGlyphAtlas } from '@codexo/exojs'`
A shared atlas that rasterizes glyphs on demand into an offscreen canvas
and wraps it as a Texture for use by the Mesh-based Text renderer.

Glyphs are always rasterized in white so that runtime tinting via
`Mesh.tint` applies the fill color without requiring re-rasterization.

Use `getDefaultGlyphAtlas()` from `atlas-singleton.ts` rather than
constructing directly.
## Constructors

- `new(width: number, height: number): DynamicGlyphAtlas<>`

## Methods

- `clear(): void`
- `getGlyph(char: string, family: string, size: number, weight: string | number, style: "normal" | "italic"): GlyphInfo`

## Properties

- `texture: Texture<>`
Source: [src/rendering/text/DynamicGlyphAtlas.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/text/DynamicGlyphAtlas.ts#L68)
