---
title: "ColorGradient"
description: "Piecewise-linear color gradient sampled by lifetime ratio `t` in `[0, 1]`. Keyframes are stored sorted by `t`; sampling outside the range returns the nearest endpoint color. Like Curve, the last accessed segment is cached so monotonically advancing `t` is O(1) amortised. Two output paths: - evaluate — writes into a `Color` instance (own scratch by default). - evaluateRgba — returns the packed `0xAABBGGRR` u32 directly, skipping the Color object. Use this in tight per-particle inner loops that write into a `Uint32Array` instance buffer."
symbol: "ColorGradient"
kind: "class"
subsystem: "particles"
importPath: "@codexo/exojs-particles"
memberCount: 3
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Source"]
sourcePath: "packages/exojs-particles/src/distributions/ColorGradient.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/distributions/ColorGradient.ts"
---
## Import

`import { ColorGradient } from '@codexo/exojs-particles'`

Piecewise-linear color gradient sampled by lifetime ratio `t` in `[0, 1]`.
Keyframes are stored sorted by `t`; sampling outside the range returns
the nearest endpoint color. Like Curve, the last accessed segment
is cached so monotonically advancing `t` is O(1) amortised.

Two output paths:
- evaluate — writes into a `Color` instance (own scratch by default).
- evaluateRgba — returns the packed `0xAABBGGRR` u32 directly,
  skipping the Color object. Use this in tight per-particle inner loops
  that write into a `Uint32Array` instance buffer.

## Constructors

- `new(keys: readonly ColorGradientKey[]): ColorGradient`

## Methods

- `evaluate(t: number, out: Color): Color`
- `evaluateRgba(t: number): number` - Returns the gradient sample at t packed into a single 32-bit RGBA integer (0xAABBGGRR). Avoids the Color object on the hot path; suitable for direct write into a Uint32Array instance buffer.

## Source

[packages/exojs-particles/src/distributions/ColorGradient.ts](https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/distributions/ColorGradient.ts)
