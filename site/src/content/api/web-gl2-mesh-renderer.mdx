---
title: "WebGl2MeshRenderer"
description: "Base class for WebGL2 renderers. Manages the connect/disconnect lifecycle and provides a safe `getBackend()` accessor that throws if the renderer is not connected. Subclasses must implement: - onConnect(backend): set up GL resources - onDisconnect(): tear down GL resources - render(drawable): batch or immediately draw the given drawable - flush(): submit any batched draw calls to the GPU"
symbol: "WebGl2MeshRenderer"
subsystem: "rendering"
importPath: "@codexo/exojs"
sourcePath: "src/rendering/webgl2/WebGl2MeshRenderer.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgl2/WebGl2MeshRenderer.ts#L32"
---
# WebGl2MeshRenderer
`import { WebGl2MeshRenderer } from '@codexo/exojs'`
Base class for WebGL2 renderers.

Manages the connect/disconnect lifecycle and provides a safe
`getBackend()` accessor that throws if the renderer is not connected.

Subclasses must implement:
- onConnect(backend): set up GL resources
- onDisconnect(): tear down GL resources
- render(drawable): batch or immediately draw the given drawable
- flush(): submit any batched draw calls to the GPU
## Constructors

- `new(): WebGl2MeshRenderer<>`

## Methods

- `connect(backend: WebGl2Backend<>): void`
- `destroy(): void`
- `disconnect(): void`
- `flush(): void`
- `getBackend(): WebGl2Backend<>`
- `getBackendOrNull(): WebGl2Backend<> | null`
- `onConnect(backend: WebGl2Backend<>): void`
- `onDisconnect(): void`
- `render(mesh: Mesh<>): void`

## Properties

- `backendType: WebGl2`
Source: [src/rendering/webgl2/WebGl2MeshRenderer.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgl2/WebGl2MeshRenderer.ts#L32)
