---
title: "SubtitleAsset"
description: "Dispatch token for subtitle loading (WebVTT and SRT). `loader.load(SubtitleAsset, 'subs.vtt')` returns `Promise<VTTCue[]>`. `loader.load(SubtitleAsset, 'subs.srt')` returns `Promise<VTTCue[]>`. Format is detected from the file extension; unknown extensions default to VTT."
symbol: "SubtitleAsset"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 1
tier: "stable"
sections: ["Import", "Constructors", "Source"]
sourcePath: "src/resources/tokens.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/tokens.ts"
---
## Import

`import { SubtitleAsset } from '@codexo/exojs'`

Dispatch token for subtitle loading (WebVTT and SRT).

`loader.load(SubtitleAsset, 'subs.vtt')` returns `Promise\<VTTCue[]>`.
`loader.load(SubtitleAsset, 'subs.srt')` returns `Promise\<VTTCue[]>`.
Format is detected from the file extension; unknown extensions default to VTT.

## Constructors

- `new(): SubtitleAsset`

## Source

[src/resources/tokens.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/tokens.ts)
