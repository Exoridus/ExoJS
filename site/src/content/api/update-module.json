{
  "title": "UpdateModule",
  "description": "Per-frame, per-batch mutator. Operates on the system's SoA storage directly — typically a single tight loop over `[0, system.liveCount)` that reads/writes the relevant `Float32Array`s. Implementations must always provide a CPU `apply()`. To make a module GPU-eligible (executed inside the system's composite compute shader on WebGPU backends), additionally implement wgsl and writeUniforms. Modules that declare a WgslContribution may also opt to declare a 1D texture binding via the `textures` field (used by `Curve` / `ColorGradient`-driven modules) — in which case uploadTextures runs once at compile time to upload the data. Implementation pattern (CPU-only module): ```ts class MyModule extends UpdateModule { apply(system, dt) { const { velX, velY, liveCount } = system; for (let i = 0; i < liveCount; i++) { velX[i] *= 0.99; velY[i] *= 0.99; } } } ``` Implementation pattern (GPU-eligible module): ```ts class MyForce extends UpdateModule { constructor(public ax: number, public ay: number) { super(); } apply(system, dt) { const { velX, velY, liveCount } = system; for (let i = 0; i < liveCount; i++) { velX[i] += this.ax * dt; velY[i] += this.ay * dt; } } wgsl(): WgslContribution { return { key: 'MyForce', uniforms: [{ name: 'ax', type: 'f32' }, { name: 'ay', type: 'f32' }], body: `velX[idx] += u_MyForce.ax * dt; velY[idx] += u_MyForce.ay * dt;`, }; } writeUniforms(view, offset) { view.setFloat32(offset + 0, this.ax, true); view.setFloat32(offset + 4, this.ay, true); } } ``` If *any* registered update module on a system lacks `wgsl()`, the system forces CPU mode regardless of backend — preserving the contract that `apply()` is always honoured. Built-in modules ship both implementations; custom modules can opt into GPU acceleration at their authors' discretion. Update modules run after integration each frame. Multiple modules execute in registration order; later modules see the effects of earlier ones.",
  "symbol": "UpdateModule",
  "kind": "class",
  "subsystem": "particles",
  "importPath": "@codexo/exojs-particles",
  "tier": "stable",
  "memberCount": 6,
  "counts": {
    "constructors": 1,
    "methods": 5,
    "properties": 0,
    "events": 0
  },
  "sections": [
    {
      "id": "import",
      "title": "Import",
      "members": [],
      "paragraphs": [
        "Per-frame, per-batch mutator. Operates on the system's SoA storage directly — typically a single tight loop over `[0, system.liveCount)` that reads/writes the relevant `Float32Array`s.",
        "Implementations must always provide a CPU `apply()`. To make a module GPU-eligible (executed inside the system's composite compute shader on WebGPU backends), additionally implement wgsl and writeUniforms. Modules that declare a WgslContribution may also opt to declare a 1D texture binding via the `textures` field (used by `Curve` / `ColorGradient`-driven modules) — in which case uploadTextures runs once at compile time to upload the data.",
        "Implementation pattern (CPU-only module):",
        "```ts class MyModule extends UpdateModule { apply(system, dt) { const { velX, velY, liveCount } = system; for (let i = 0; i < liveCount; i++) { velX[i] *= 0.99; velY[i] *= 0.99; } } } ```",
        "Implementation pattern (GPU-eligible module):",
        "```ts class MyForce extends UpdateModule { constructor(public ax: number, public ay: number) { super(); }",
        "apply(system, dt) { const { velX, velY, liveCount } = system; for (let i = 0; i < liveCount; i++) { velX[i] += this.ax * dt; velY[i] += this.ay * dt; } }",
        "wgsl(): WgslContribution { return { key: 'MyForce', uniforms: [{ name: 'ax', type: 'f32' }, { name: 'ay', type: 'f32' }], body: `velX[idx] += u_MyForce.ax * dt; velY[idx] += u_MyForce.ay * dt;`, }; }",
        "writeUniforms(view, offset) { view.setFloat32(offset + 0, this.ax, true); view.setFloat32(offset + 4, this.ay, true); } } ```",
        "If *any* registered update module on a system lacks `wgsl()`, the system forces CPU mode regardless of backend — preserving the contract that `apply()` is always honoured. Built-in modules ship both implementations; custom modules can opt into GPU acceleration at their authors' discretion.",
        "Update modules run after integration each frame. Multiple modules execute in registration order; later modules see the effects of earlier ones."
      ],
      "importLine": "import { UpdateModule } from '@codexo/exojs-particles'",
      "sourceLink": null
    },
    {
      "id": "constructors",
      "title": "Constructors",
      "members": [
        {
          "name": "new",
          "signature": "new(): UpdateModule",
          "params": [],
          "returnType": "UpdateModule",
          "description": ""
        }
      ],
      "paragraphs": [],
      "importLine": null,
      "sourceLink": null
    },
    {
      "id": "methods",
      "title": "Methods",
      "members": [
        {
          "name": "apply",
          "signature": "apply(system: ParticleSystem, dt: number): void",
          "params": [
            {
              "name": "system",
              "type": "ParticleSystem",
              "optional": false
            },
            {
              "name": "dt",
              "type": "number",
              "optional": false
            }
          ],
          "returnType": "void",
          "description": ""
        },
        {
          "name": "destroy",
          "signature": "destroy(): void",
          "params": [],
          "returnType": "void",
          "description": "Optional cleanup hook called from ParticleSystem.destroy."
        },
        {
          "name": "uploadTextures",
          "signature": "uploadTextures(device: GPUDevice, textures: ReadonlyMap<string, GPUTexture>): void",
          "params": [
            {
              "name": "device",
              "type": "GPUDevice",
              "optional": false
            },
            {
              "name": "textures",
              "type": "ReadonlyMap<string, GPUTexture>",
              "optional": false
            }
          ],
          "returnType": "void",
          "description": "Upload texture data (Curve/ColorGradient lookup tables) to the GPU at compile time. Receives the GPUDevice and a map of texture bindings keyed by the name in WgslContribution.textures. Called once after pipeline creation. Required when wgsl declares textures."
        },
        {
          "name": "wgsl",
          "signature": "wgsl(): WgslContribution",
          "params": [],
          "returnType": "WgslContribution",
          "description": "Override to declare a GPU contribution. Returning a WgslContribution makes this module GPU-eligible; omitting (or returning undefined) forces CPU mode for any system that uses this module."
        },
        {
          "name": "writeUniforms",
          "signature": "writeUniforms(view: DataView, byteOffset: number, dt: number): void",
          "params": [
            {
              "name": "view",
              "type": "DataView",
              "optional": false
            },
            {
              "name": "byteOffset",
              "type": "number",
              "optional": false
            },
            {
              "name": "dt",
              "type": "number",
              "optional": false
            }
          ],
          "returnType": "void",
          "description": "Write this module's current uniform values into the shared uniform buffer at byteOffset. Layout must match the field declarations returned by wgsl (in the same order). Receives the current frame dt (seconds). Modules tracking accumulated time (e.g. noise/turbulence) should advance their internal counter here to stay in sync when running in GPU mode (where apply is not called). Required when wgsl declares uniforms. Called every frame by the system before dispatching compute."
        }
      ],
      "paragraphs": [],
      "importLine": null,
      "sourceLink": null
    },
    {
      "id": "source",
      "title": "Source",
      "members": [],
      "paragraphs": [],
      "importLine": null,
      "sourceLink": {
        "label": "packages/exojs-particles/src/modules/UpdateModule.ts",
        "href": "https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/modules/UpdateModule.ts"
      }
    }
  ],
  "sourcePath": "packages/exojs-particles/src/modules/UpdateModule.ts",
  "sourceUrl": "https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/modules/UpdateModule.ts"
}
