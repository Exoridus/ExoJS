{
  "title": "UniformSchemaOptions",
  "description": "A uniform declaration on a ShaderSource. `uniforms` declares one engine-named block and is the common case; `uniformBlocks` declares one or more named blocks explicitly. The two are alternatives and cannot both be given.",
  "symbol": "UniformSchemaOptions",
  "kind": "interface",
  "subsystem": "rendering",
  "importPath": "@codexo/exojs",
  "tier": "stable",
  "memberCount": 2,
  "counts": {
    "constructors": 0,
    "methods": 0,
    "properties": 2,
    "events": 0
  },
  "sections": [
    {
      "id": "import",
      "title": "Import",
      "members": [],
      "paragraphs": [
        "A uniform declaration on a ShaderSource.",
        "`uniforms` declares one engine-named block and is the common case; `uniformBlocks` declares one or more named blocks explicitly. The two are alternatives and cannot both be given."
      ],
      "importLine": "import { UniformSchemaOptions } from '@codexo/exojs'",
      "sourceLink": null
    },
    {
      "id": "properties",
      "title": "Properties",
      "members": [
        {
          "name": "uniformBlocks",
          "signature": "uniformBlocks?: B",
          "signatureTokens": [
            {
              "text": "uniformBlocks",
              "kind": "name"
            },
            {
              "text": "?",
              "kind": "punctuation"
            },
            {
              "text": ": ",
              "kind": "punctuation"
            },
            {
              "text": "B",
              "kind": "type"
            }
          ],
          "params": [],
          "returnType": null,
          "description": "Named blocks the shader reads through their record keys. Blocks take bindings 0..n-1 of the consumer's user bind group in declaration order, with texture bindings following after them."
        },
        {
          "name": "uniforms",
          "signature": "uniforms?: F",
          "signatureTokens": [
            {
              "text": "uniforms",
              "kind": "name"
            },
            {
              "text": "?",
              "kind": "punctuation"
            },
            {
              "text": ": ",
              "kind": "punctuation"
            },
            {
              "text": "F",
              "kind": "type"
            }
          ],
          "params": [],
          "returnType": null,
          "description": "Fields of a single block the shader reads through the instance name uniforms. Declaring it switches materials and filters built on this source to typed accessors and generates the block's GLSL and WGSL declarations."
        }
      ],
      "paragraphs": [],
      "importLine": null,
      "sourceLink": null
    },
    {
      "id": "source",
      "title": "Source",
      "members": [],
      "paragraphs": [],
      "importLine": null,
      "sourceLink": {
        "label": "src/rendering/uniforms/uniformSchema.ts",
        "href": "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/uniforms/uniformSchema.ts"
      }
    }
  ],
  "sourcePath": "src/rendering/uniforms/uniformSchema.ts",
  "sourceUrl": "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/uniforms/uniformSchema.ts"
}
