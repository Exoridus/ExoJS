---
title: "Signal"
description: "Lightweight typed event emitter. Each `Signal` represents one named notification channel (e.g. `onResize`, `onFrame`). Listeners are added with Signal.add or Signal.once, removed with Signal.remove, and notified with Signal.dispatch. `Args` is the tuple of arguments passed to listeners — type-checked end to end so a `new Signal<[number, string]>()` enforces both `dispatch(1, 'x')` and the listener signature `(n: number, s: string) => …`. Handlers are stored as direct function references (no wrapper objects). `dispatch` uses a guard flag instead of a snapshot copy, so no allocation occurs per dispatch. Handlers added or removed during dispatch take effect on the next call; a `remove` mid-dispatch defers the splice until after the current iteration finishes. Returning `false` from a handler short-circuits the rest of the dispatch."
symbol: "Signal"
kind: "class"
subsystem: "core"
importPath: "@codexo/exojs"
memberCount: 9
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/core/Signal.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/core/Signal.ts"
---
## Import

`import { Signal } from '@codexo/exojs'`

Lightweight typed event emitter. Each `Signal` represents one named
notification channel (e.g. `onResize`, `onFrame`). Listeners are added with
Signal.add or Signal.once, removed with
Signal.remove, and notified with Signal.dispatch.

`Args` is the tuple of arguments passed to listeners — type-checked end to
end so a `new Signal\<[number, string]>()` enforces both `dispatch(1, 'x')`
and the listener signature `(n: number, s: string) => …`.

Handlers are stored as direct function references (no wrapper objects).
`dispatch` uses a guard flag instead of a snapshot copy, so no allocation
occurs per dispatch. Handlers added or removed during dispatch take effect
on the next call; a `remove` mid-dispatch defers the splice until after the
current iteration finishes. Returning `false` from a handler short-circuits
the rest of the dispatch.

## Constructors

- `new(): Signal<Args>`

## Methods

- `add(handler: SignalHandler<Args>): this` - Register a listener. Idempotent — adding the same handler reference twice is a no-op. Use arrow functions or pre-bound methods to ensure correct this inside the handler.
- `clear(): this` - Remove every listener.
- `destroy(): void`
- `dispatch(params: Args): this` - Notify every registered listener in registration order. Returning false from a handler stops dispatch to the remaining listeners for this call. Listeners may safely remove themselves or others during dispatch — removals are deferred until after the current iteration completes.
- `has(handler: SignalHandler<Args>): boolean` - true when handler is currently registered.
- `once(handler: SignalHandler<Args>): this` - Register a listener that auto-removes itself after the first dispatch. The internal wrapper reference differs from handler, so calling Signal.remove with the original handler reference does NOT remove it — use Signal.clear to undo a once registration.
- `remove(handler: SignalHandler<Args>): this` - Remove a previously registered handler. No-op if absent.

## Properties

- `count: number` - Number of currently registered listeners.

## Source

[src/core/Signal.ts](https://github.com/Exoridus/ExoJS/blob/main/src/core/Signal.ts)
