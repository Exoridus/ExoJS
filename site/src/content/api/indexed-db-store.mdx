---
title: "IndexedDbStore"
description: "CacheStore implementation that persists processed asset data in an IndexedDB database via IndexedDbDatabase. Pass an instance to LoaderOptions.cache to enable cross-session asset caching with no additional configuration beyond a database name."
symbol: "IndexedDbStore"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 6
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Source"]
sourcePath: "src/resources/IndexedDbStore.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/IndexedDbStore.ts"
---
## Import

`import { IndexedDbStore } from '@codexo/exojs'`

CacheStore implementation that persists processed asset data in an
IndexedDB database via IndexedDbDatabase.

Pass an instance to LoaderOptions.cache to enable cross-session
asset caching with no additional configuration beyond a database name.

## Constructors

- `new(nameOrOptions: string | IndexedDbStoreOptions): IndexedDbStore`

## Methods

- `clear(storageName: string): Promise<boolean>` - Removes all entries within storageName. Returns true on success.
- `delete(storageName: string, key: string): Promise<boolean>` - Removes a single entry. Returns true if the entry existed and was removed.
- `destroy(): void` - Synchronously releases any open handles held by the store.
- `load(storageName: string, key: string): Promise<unknown>` - Loads a previously saved value, or null if the entry does not exist.
- `save(storageName: string, key: string, data: unknown): Promise<void>` - Persists data under the given namespace and key. Implementations must tolerate failures silently when storage is full.

## Source

[src/resources/IndexedDbStore.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/IndexedDbStore.ts)
