---
title: "JoyConLeftGamepadMapping"
description: "Mapping for the Nintendo Joy-Con (L) held horizontally as a solo controller. Declares only channels that physically exist on the device — one stick (mapped to GamepadAxis.LeftStickX / `LeftStickY` and the direction-split equivalents), four face buttons, the SL/SR inner shoulders (routed through the standard shoulder channels), Minus, the Capture button, and the stick-click. Right-stick channels, triggers, Plus/Home, Touchpad, paddles, and auxiliary axes are intentionally absent. Use GamepadMapping.hasChannel to detect availability before binding inputs that may not exist on every device family."
symbol: "JoyConLeftGamepadMapping"
kind: "class"
subsystem: "input"
importPath: "@codexo/exojs"
memberCount: 6
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/input/JoyConLeftGamepadMapping.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/input/JoyConLeftGamepadMapping.ts"
---
## Import

`import { JoyConLeftGamepadMapping } from '@codexo/exojs'`

Mapping for the Nintendo Joy-Con (L) held horizontally as a solo controller.

Declares only channels that physically exist on the device — one stick
(mapped to GamepadAxis.LeftStickX / `LeftStickY` and the
direction-split equivalents), four face buttons, the SL/SR inner shoulders
(routed through the standard shoulder channels), Minus, the Capture
button, and the stick-click.

Right-stick channels, triggers, Plus/Home, Touchpad, paddles, and
auxiliary axes are intentionally absent. Use
GamepadMapping.hasChannel to detect availability before binding
inputs that may not exist on every device family.

## Constructors

- `new(): JoyConLeftGamepadMapping`

## Methods

- `destroy(): void` - Releases all button and axis control references held by this mapping. Call when the associated gamepad disconnects to allow garbage collection.
- `hasChannel(channel: GamepadButtonChannel | GamepadAxisChannel): boolean` - Returns true when this mapping declares at least one button or axis control that writes to channel. Use to detect device-specific capabilities at runtime — e.g. before binding an input to a right-stick channel that may not exist on a single Joy-Con.

## Properties

- `axes: readonly GamepadAxis[]` - Ordered list of axes, indexed by the Gamepad API axis index.
- `buttons: readonly GamepadButton[]` - Ordered list of buttons, indexed by the Gamepad API button index.
- `family: JoyConLeft` - Identifies the device family this mapping targets.

## Source

[src/input/JoyConLeftGamepadMapping.ts](https://github.com/Exoridus/ExoJS/blob/main/src/input/JoyConLeftGamepadMapping.ts)
