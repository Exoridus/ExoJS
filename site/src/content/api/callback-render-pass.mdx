---
title: "CallbackRenderPass"
description: "Runs a user callback as one pass. The callback receives the RenderingContext (high-level: `context.render(node)`, `context.backend` immediate draws, etc.). Set `options.target` to redirect the callback's output into an off-screen RenderTexture (immediate-mode \"scene\" → texture). To run a low-level BackendRenderPass inside a pipeline, bridge it here: `new CallbackRenderPass((context) => context.backend.execute(myBackendPass))`. `view`/`target`/`clear` are fixed at construction; the off-screen redirect is built once and reused every frame (no per-frame allocation). If your callback closes over owned GPU resources, subclass RenderPass instead so you can override `destroy()`."
symbol: "CallbackRenderPass"
kind: "class"
subsystem: "rendering"
importPath: "@codexo/exojs"
memberCount: 6
tier: "advanced"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/rendering/CallbackRenderPass.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/CallbackRenderPass.ts#L37"
---
## Import

`import { CallbackRenderPass } from '@codexo/exojs'`

Runs a user callback as one pass. The callback receives the RenderingContext (high-level:
`context.render(node)`, `context.backend` immediate draws, etc.). Set `options.target` to redirect the callback's
output into an off-screen RenderTexture (immediate-mode "scene" → texture).

To run a low-level BackendRenderPass inside a pipeline, bridge it here:
`new CallbackRenderPass((context) => context.backend.execute(myBackendPass))`.

`view`/`target`/`clear` are fixed at construction; the off-screen redirect is built once and reused every frame
(no per-frame allocation). If your callback closes over owned GPU resources, subclass RenderPass instead so
you can override `destroy()`.

## Constructors

- `new(callback: object, options?: CallbackRenderPassOptions): CallbackRenderPass`

## Methods

- `destroy(): void`
- `execute(context: RenderingContext): void`
- `resize(_width: number, _height: number): void`

## Properties

- `enabled: boolean`
- `label: string`

## Source

[src/rendering/CallbackRenderPass.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/CallbackRenderPass.ts#L37)
