---
title: "WebGpuComputePipeline"
description: "Owning wrapper around a `GPUComputePipeline` plus its bind-group layout. Created once per shader; multiple bind groups can be bound per dispatch to render different particle systems with the same pipeline. Construct via create; do not call `new GPUComputePipeline` directly elsewhere."
symbol: "WebGpuComputePipeline"
kind: "class"
subsystem: "rendering"
importPath: "@codexo/exojs"
memberCount: 8
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/rendering/webgpu/compute/WebGpuComputePipeline.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgpu/compute/WebGpuComputePipeline.ts#L25"
---
## Import

`import { WebGpuComputePipeline } from '@codexo/exojs'`

Owning wrapper around a `GPUComputePipeline` plus its bind-group layout.
Created once per shader; multiple bind groups can be bound per dispatch
to render different particle systems with the same pipeline.

Construct via create; do not call `new GPUComputePipeline`
directly elsewhere.

## Constructors

- `new(device: GPUDevice, pipeline: GPUComputePipeline, bindGroupLayout: GPUBindGroupLayout, workgroupSize: number): WebGpuComputePipeline`

## Methods

- `createBindGroup(entries: readonly object[], label?: string): GPUBindGroup`
- `dispatch(passEncoder: GPUComputePassEncoder, itemCount: number): void`
- `create(device: GPUDevice, opts: object): WebGpuComputePipeline`

## Properties

- `bindGroupLayout: GPUBindGroupLayout`
- `device: GPUDevice`
- `pipeline: GPUComputePipeline`
- `workgroupSize: number`

## Source

[src/rendering/webgpu/compute/WebGpuComputePipeline.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgpu/compute/WebGpuComputePipeline.ts#L25)
