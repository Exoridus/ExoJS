---
title: "Flags"
description: "Type-safe bitfield utility for managing sets of numeric enum flags. `T` should be a numeric const-enum or a type whose values are `number`. Internally stores the combined flags as a single 32-bit integer."
symbol: "Flags"
kind: "class"
subsystem: "math"
importPath: "@codexo/exojs"
memberCount: 7
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Source"]
sourcePath: "src/math/Flags.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/math/Flags.ts#L17"
---
## Import

`import { Flags } from '@codexo/exojs'`

Type-safe bitfield utility for managing sets of numeric enum flags.

`T` should be a numeric const-enum or a type whose values are `number`.
Internally stores the combined flags as a single 32-bit integer.

## Constructors

- `new(flags: number[]): Flags<T>`

## Methods

- `clear(): this`
- `destroy(): void`
- `has(flags: V[]): boolean`
- `pop(flag: V): boolean`
- `push(flags: V[]): this`
- `remove(flags: V[]): this`

## Source

[src/math/Flags.ts](https://github.com/Exoridus/ExoJS/blob/main/src/math/Flags.ts#L17)
