---
title: "Sound"
description: "Pre-decoded short audio clip backed by an `AudioBuffer`. Sound is a **data descriptor** — it holds the decoded audio buffer, sprite definitions, default playback parameters, and spatial configuration but does NOT start playback itself. Playback is driven by `AudioManager.play(sound, options)` which returns a Voice handle. Multiple concurrent plays of the same Sound are supported up to `poolSize`. When the pool is full the configured SoundPoolStrategy decides which active voice to evict. Use AudioStream for long-form streaming audio (single source, decoded lazily) — `Sound` is best for short, frequently-triggered clips."
symbol: "Sound"
kind: "class"
subsystem: "audio"
importPath: "@codexo/exojs"
memberCount: 25
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/audio/Sound.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/audio/Sound.ts"
---
## Import

`import { Sound } from '@codexo/exojs'`

Pre-decoded short audio clip backed by an `AudioBuffer`.

Sound is a **data descriptor** — it holds the decoded audio buffer, sprite
definitions, default playback parameters, and spatial configuration but does
NOT start playback itself. Playback is driven by
`AudioManager.play(sound, options)` which returns a Voice handle.

Multiple concurrent plays of the same Sound are supported up to `poolSize`.
When the pool is full the configured SoundPoolStrategy decides which
active voice to evict.

Use AudioStream for long-form streaming audio (single source, decoded
lazily) — `Sound` is best for short, frequently-triggered clips.

## Constructors

- `new(audioBuffer: AudioBuffer, options: SoundOptions): Sound`

## Methods

- `_createVoice(manager: AudioManager, options: PlayOptions): Voice` - Implements Playable. Called by AudioManager.play; do not call directly — use manager.play(sound, options) instead. Creates one SoundVoice backed by a single AudioBufferSourceNode. Pool limits are enforced: if the pool is full the configured eviction strategy picks a victim to stop before the new voice starts.
- `_stopAllVoices(): void` - Stop all currently active voices (e.g. for replace mode).
- `clip(offset: number, duration: number): Sound` - Return a new Sound that plays only the [offset, offset + duration] sub-range (seconds) of this sound's buffer — an audio atlas / sprite-sheet clip. The clip shares the same decoded AudioBuffer (no extra memory) and inherits this sound's default playback + spatial settings, including its own independent voice pool.
- `defineSprite(name: string, clip: AudioSpriteClip): this`
- `destroy(): void`
- `hasSprite(name: string): boolean`
- `removeSprite(name: string): this`
- `setPoolSize(poolSize: number): this`
- `setSprites(sprites: Readonly<Record<string, AudioSpriteClip>>): this`

## Properties

- `loop: boolean` - Default loop flag applied to new voices.
- `muted: boolean` - Default muted flag applied to new voices.
- `playbackRate: number` - Default playback rate applied to new voices.
- `volume: number` - Default volume applied to new voices.
- `audioBuffer: AudioBuffer` - The underlying decoded audio data. Useful for sharing a single decoded buffer across multiple Sound instances.
- `distanceModel: DistanceModel` - Distance-attenuation model.
- `duration: number` - Playable duration in seconds — the full buffer, or the clip span for a Sound.clip.
- `maxDistance: number` - Maximum distance for the 'linear' model — volume reaches zero here.
- `poolSize: number`
- `poolStrategy: SoundPoolStrategy` - The eviction strategy used when the pool is at capacity.
- `position: Vector | null`
- `priority: number` - Sound priority. Used by the LowestPriority pool strategy. Higher values indicate higher priority (less likely to be evicted).
- `refDistance: number` - Reference distance — volume is at full strength at and below this distance.
- `rolloffFactor: number` - Falloff steepness. Higher values attenuate faster with distance.
- `velocity: Vector | null`

## Source

[src/audio/Sound.ts](https://github.com/Exoridus/ExoJS/blob/main/src/audio/Sound.ts)
