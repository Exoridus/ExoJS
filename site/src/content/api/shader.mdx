---
title: "Shader"
description: "Backend-agnostic shader program descriptor. Holds raw GLSL source strings and, after connect is called, a live ShaderProgram with populated attributes and uniforms maps. Call bind before draw calls and sync after updating uniform values. Both maps are cleared on disconnect and destroy."
symbol: "Shader"
kind: "class"
subsystem: "rendering"
importPath: "@codexo/exojs"
memberCount: 13
tier: "advanced"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/rendering/shader/Shader.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/shader/Shader.ts"
---
## Import

`import { Shader } from '@codexo/exojs'`

Backend-agnostic shader program descriptor.

Holds raw GLSL source strings and, after connect is called, a live
ShaderProgram with populated attributes and uniforms maps.
Call bind before draw calls and sync after updating uniform values.
Both maps are cleared on disconnect and destroy.

## Constructors

- `new(vertexSource: string, fragmentSource: string): Shader`

## Methods

- `bind(): this`
- `connect(program: ShaderProgram): this` - Attach a backend GPU program, compile/link it, and populate the attribute and uniform maps. Must be called before bind, sync, or accessing attributes/uniforms.
- `destroy(): void`
- `disconnect(): this`
- `getAttribute(name: string): ShaderAttribute` - Retrieve a named vertex attribute from the map populated by the backend.
- `getUniform(name: string): ShaderUniform` - Retrieve a named uniform from the map populated by the backend.
- `sync(): this` - Upload all dirty ShaderUniform values to the GPU. Call once per draw call after modifying uniform data.
- `unbind(): this`

## Properties

- `attributes: Map<string, ShaderAttribute>` - Vertex attribute metadata populated by the backend after connect.
- `uniforms: Map<string, ShaderUniform>` - Uniform metadata populated by the backend after connect.
- `fragmentSource: string`
- `vertexSource: string`

## Source

[src/rendering/shader/Shader.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/shader/Shader.ts)
