---
title: "AbstractMedia"
description: "Shared base for Sound and Music: holds playback primitives (volume, loop, playbackRate, muted, currentTime), the AudioBus routing slot, lifecycle Signals (`onStart`, `onStop`), and ramp helpers (AbstractMedia.fadeIn, AbstractMedia.fadeOut) that operate on whatever `GainNode` the subclass exposes. Subclasses implement the abstract members to bind to either an `AudioBufferSourceNode` (Sound) or `MediaElementAudioSourceNode` (Music)."
symbol: "AbstractMedia"
kind: "class"
subsystem: "audio"
importPath: "@codexo/exojs"
memberCount: 32
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Events", "Source"]
sourcePath: "src/audio/AbstractMedia.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/audio/AbstractMedia.ts#L22"
---
## Import

`import { AbstractMedia } from '@codexo/exojs'`

Shared base for Sound and Music: holds playback
primitives (volume, loop, playbackRate, muted, currentTime), the
AudioBus routing slot, lifecycle Signals (`onStart`, `onStop`),
and ramp helpers (AbstractMedia.fadeIn, AbstractMedia.fadeOut)
that operate on whatever `GainNode` the subclass exposes.

Subclasses implement the abstract members to bind to either an
`AudioBufferSourceNode` (Sound) or `MediaElementAudioSourceNode` (Music).

## Constructors

- `new(initialState: AbstractMediaInitialState): AbstractMedia`

## Methods

- `_connectToBus(): void`
- `_defaultBus(): AudioBus`
- `_disconnectFromBus(): void`
- `_getAudioSetup(): object | null`
- `applyOptions(options: Partial<PlaybackOptions>): this`
- `destroy(): void`
- `fadeIn(durationMs: number): this`
- `fadeOut(durationMs: number, options: object): this`
- `getTime(): number`
- `pause(options?: Partial<PlaybackOptions>): this`
- `play(options?: Partial<PlaybackOptions>): this`
- `setLoop(loop: boolean): this`
- `setMuted(muted: boolean): this`
- `setPlaybackRate(playbackRate: number): this`
- `setTime(time: number): this`
- `setVolume(volume: number): this`
- `stop(options?: Partial<PlaybackOptions>): this`
- `toggle(options?: Partial<PlaybackOptions>): this`

## Properties

- `analyserTarget: AudioNode | null`
- `bus: AudioBus`
- `currentTime: number`
- `duration: number`
- `loop: boolean`
- `muted: boolean`
- `paused: boolean`
- `playbackRate: number`
- `playing: boolean`
- `progress: number`
- `volume: number`

## Events

- `onStart: Signal<[]>`
- `onStop: Signal<[]>`

## Source

[src/audio/AbstractMedia.ts](https://github.com/Exoridus/ExoJS/blob/main/src/audio/AbstractMedia.ts#L22)
