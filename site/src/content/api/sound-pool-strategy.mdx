---
title: "SoundPoolStrategy"
description: "Eviction strategy used when the pool is full and a new play is requested. At the per-Sound level all pooled instances share the same priority, so `LowestPriority` degenerates to `FirstInFirstOut` in V1. The enum is forward-compatible with a future global voice manager that culls across multiple Sound instances."
symbol: "SoundPoolStrategy"
kind: "enum"
subsystem: "audio"
importPath: "@codexo/exojs"
memberCount: 3
tier: "stable"
sections: ["Import", "Members", "Source"]
sourcePath: "src/audio/Sound.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/audio/Sound.ts#L18"
---
## Import

`import { SoundPoolStrategy } from '@codexo/exojs'`

Eviction strategy used when the pool is full and a new play is requested.

At the per-Sound level all pooled instances share the same priority, so
`LowestPriority` degenerates to `FirstInFirstOut` in V1. The enum is
forward-compatible with a future global voice manager that culls across
multiple Sound instances.

## Members

- `FirstInFirstOut`
- `LeastRecentlyUsed`
- `LowestPriority`

## Source

[src/audio/Sound.ts](https://github.com/Exoridus/ExoJS/blob/main/src/audio/Sound.ts#L18)
