---
title: "LowpassFilter"
description: "Low-pass filter backed by a `BiquadFilterNode` in `lowpass` mode. Attenuates frequencies above the cutoff and passes everything below it. Useful for muffling audio (e.g., sound heard through a wall), taming harsh high-frequency content, or creating underwater/telephone effects. The `resonance` (Q) parameter controls the sharpness of the roll-off peak."
symbol: "LowpassFilter"
kind: "class"
subsystem: "audio"
importPath: "@codexo/exojs"
memberCount: 7
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/audio/filters/LowpassFilter.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/audio/filters/LowpassFilter.ts"
---
## Import

`import { LowpassFilter } from '@codexo/exojs'`

Low-pass filter backed by a `BiquadFilterNode` in `lowpass` mode.
Attenuates frequencies above the cutoff and passes everything below it.
Useful for muffling audio (e.g., sound heard through a wall), taming
harsh high-frequency content, or creating underwater/telephone effects.
The `resonance` (Q) parameter controls the sharpness of the roll-off peak.

## Constructors

- `new(options: LowpassFilterOptions): LowpassFilter`

## Methods

- `destroy(): void` - Disconnects all audio nodes and releases resources. Must be called when the effect is no longer needed.

## Properties

- `frequency: number` - Cutoff frequency in Hz. Signals above this frequency are attenuated. Range 20..20000, default 1000.
- `inputNode: AudioNode` - The node where audio enters this effect.
- `outputNode: AudioNode` - The node where audio exits this effect.
- `ready: Promise<void>` - Resolves when the effect is fully initialized. Defaults to immediately-resolved for sync effects.
- `resonance: number` - Q (resonance) at the cutoff. Higher values create a sharper, resonant peak. Minimum 0.0001, default 1.

## Source

[src/audio/filters/LowpassFilter.ts](https://github.com/Exoridus/ExoJS/blob/main/src/audio/filters/LowpassFilter.ts)
