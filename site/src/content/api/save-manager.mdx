---
title: "SaveManager"
description: "Thin convenience over JsonStore for persisting and restoring scene layouts by named slot. Serializes the **structural state** of a scene (see Scene.serialize); pair it with your own game-state persistence for a complete save system. ```ts const saves = new SaveManager(); await saves.save('slot-1', scene); // ... later, with the same assets pre-loaded ... await saves.load('slot-1', scene); ```"
symbol: "SaveManager"
kind: "class"
subsystem: "core"
importPath: "@codexo/exojs"
memberCount: 6
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/core/serialization/SaveManager.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/core/serialization/SaveManager.ts#L19"
---
## Import

`import { SaveManager } from '@codexo/exojs'`

Thin convenience over JsonStore for persisting and restoring scene
layouts by named slot. Serializes the **structural state** of a scene (see
Scene.serialize); pair it with your own game-state persistence for a
complete save system.

```ts
const saves = new SaveManager();
await saves.save('slot-1', scene);
// ... later, with the same assets pre-loaded ...
await saves.load('slot-1', scene);
```

## Constructors

- `new(store: JsonStore): SaveManager`

## Methods

- `delete(slot: string): Promise<boolean>`
- `has(slot: string): Promise<boolean>`
- `load(slot: string, scene: Scene): Promise<boolean>`
- `save(slot: string, scene: Scene): Promise<void>`

## Properties

- `store: JsonStore`

## Source

[src/core/serialization/SaveManager.ts](https://github.com/Exoridus/ExoJS/blob/main/src/core/serialization/SaveManager.ts#L19)
