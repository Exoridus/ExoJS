---
title: "AudioFilter"
description: "Abstract base for all audio processing filters in ExoJS. Each filter exposes stable inputNode and outputNode AudioNodes that the audio bus connects into the Web Audio graph. Async filters (e.g., worklet-based) expose a ready promise that resolves once the filter is fully initialized."
symbol: "AudioFilter"
kind: "class"
subsystem: "audio"
importPath: "@codexo/exojs"
memberCount: 5
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/audio/AudioFilter.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/audio/AudioFilter.ts#L7"
---
## Import

`import { AudioFilter } from '@codexo/exojs'`

Abstract base for all audio processing filters in ExoJS. Each filter exposes
stable inputNode and outputNode AudioNodes that the audio bus
connects into the Web Audio graph. Async filters (e.g., worklet-based) expose
a ready promise that resolves once the filter is fully initialized.

## Constructors

- `new(): AudioFilter`

## Methods

- `destroy(): void`

## Properties

- `inputNode: AudioNode`
- `outputNode: AudioNode`
- `ready: Promise<void>`

## Source

[src/audio/AudioFilter.ts](https://github.com/Exoridus/ExoJS/blob/main/src/audio/AudioFilter.ts#L7)
