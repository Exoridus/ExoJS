---
title: "ColorOverSpeed"
description: "Per-frame, per-particle color sampler driven by velocity magnitude rather than lifetime ratio. Each live particle's tint is set to the gradient evaluated at `clamp((|velocity| - minSpeed) / (maxSpeed - minSpeed), 0, 1)`. Use cases: heat-mapping (slow=blue, fast=red), velocity-tinted trails, speed-gated highlights. GPU-eligible: gradient uploaded as a 256-tap 1D RGBA8 texture, sampled with linear filtering. Replaces the full color word — pair with a separate AlphaFadeOverLifetime after this module if you want to keep alpha controlled by lifetime."
symbol: "ColorOverSpeed"
kind: "class"
subsystem: "particles"
importPath: "@codexo/exojs-particles"
memberCount: 9
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "packages/exojs-particles/src/modules/ColorOverSpeed.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/modules/ColorOverSpeed.ts#L26"
---
## Import

`import { ColorOverSpeed } from '@codexo/exojs-particles'`

Per-frame, per-particle color sampler driven by velocity magnitude rather
than lifetime ratio. Each live particle's tint is set to the gradient
evaluated at `clamp((|velocity| - minSpeed) / (maxSpeed - minSpeed), 0, 1)`.

Use cases: heat-mapping (slow=blue, fast=red), velocity-tinted trails,
speed-gated highlights.

GPU-eligible: gradient uploaded as a 256-tap 1D RGBA8 texture, sampled
with linear filtering. Replaces the full color word — pair with a
separate AlphaFadeOverLifetime after this module if you want to
keep alpha controlled by lifetime.

## Constructors

- `new(gradient: ColorGradient, minSpeed: number, maxSpeed: number): ColorOverSpeed`

## Methods

- `apply(system: ParticleSystem, _dt: number): void`
- `destroy(): void`
- `uploadTextures(device: GPUDevice, textures: ReadonlyMap<string, GPUTexture>): void`
- `wgsl(): WgslContribution`
- `writeUniforms(view: DataView, offset: number): void`

## Properties

- `gradient: ColorGradient`
- `maxSpeed: number`
- `minSpeed: number`

## Source

[packages/exojs-particles/src/modules/ColorOverSpeed.ts](https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/modules/ColorOverSpeed.ts#L26)
