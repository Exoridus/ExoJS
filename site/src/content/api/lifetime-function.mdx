---
title: "LifetimeFunction"
description: "Lifetime-parameterised function that produces a deterministic `T` for a given lifetime ratio `t` in `[0, 1]`. Used by per-frame update modules that interpolate over particle lifetime — `ColorOverLifetime` (Gradient), `ScaleOverLifetime` (Curve), etc. Mutable-target rule: same as Distribution — pass your own scratch via `out` for hot loops, omit for one-off calls."
symbol: "LifetimeFunction"
subsystem: "particles"
importPath: "@codexo/exojs"
sourcePath: "src/particles/distributions/Distribution.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/particles/distributions/Distribution.ts#L44"
---
# LifetimeFunction
`import { LifetimeFunction } from '@codexo/exojs'`
Lifetime-parameterised function that produces a deterministic `T` for a
given lifetime ratio `t` in `[0, 1]`. Used by per-frame update modules
that interpolate over particle lifetime — `ColorOverLifetime` (Gradient),
`ScaleOverLifetime` (Curve), etc.

Mutable-target rule: same as Distribution — pass your own scratch
via `out` for hot loops, omit for one-off calls.
## Properties

- `evaluate: unknown`
Source: [src/particles/distributions/Distribution.ts](https://github.com/Exoridus/ExoJS/blob/main/src/particles/distributions/Distribution.ts#L44)
