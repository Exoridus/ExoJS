---
title: "GamepadAxis"
description: "Single mappable analog axis on a physical gamepad. Holds the raw browser `Gamepad.axes[]` index, the canonical channel the value is written to, and the transform pipeline applied each frame by transformValue. Direction-split axis channels (e.g. `LeftStickLeft`, `LeftStickRight`) live in the 0..1 range — set `invert: true` on the negative half so it reads positive when pushed in its direction. Aggregate channels (e.g. `LeftStickX`, `LeftStickY`) live in the full -1..1 range — set `bipolar: true` to preserve sign through the pipeline. The static namespace exports (`GamepadAxis.LeftStickLeft`, `.LeftStickX`, ...) carry the canonical channel offsets used to address each axis."
symbol: "GamepadAxis"
kind: "class"
subsystem: "input"
importPath: "@codexo/exojs"
memberCount: 8
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/input/GamepadAxis.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/input/GamepadAxis.ts#L100"
---
## Import

`import { GamepadAxis } from '@codexo/exojs'`

Single mappable analog axis on a physical gamepad. Holds the raw browser
`Gamepad.axes[]` index, the canonical channel the value is written to, and
the transform pipeline applied each frame by transformValue.

Direction-split axis channels (e.g. `LeftStickLeft`, `LeftStickRight`)
live in the 0..1 range — set `invert: true` on the negative half so it
reads positive when pushed in its direction.

Aggregate channels (e.g. `LeftStickX`, `LeftStickY`) live in the full
-1..1 range — set `bipolar: true` to preserve sign through the pipeline.

The static namespace exports (`GamepadAxis.LeftStickLeft`,
`.LeftStickX`, ...) carry the canonical channel offsets used to address
each axis.

## Constructors

- `new(index: number, channel: GamepadAxisChannel, options: GamepadAxisOptions): GamepadAxis`

## Methods

- `transformValue(value: number): number`

## Properties

- `bipolar: boolean`
- `channel: GamepadAxisChannel`
- `index: number`
- `invert: boolean`
- `normalize: boolean`
- `threshold: number`

## Source

[src/input/GamepadAxis.ts](https://github.com/Exoridus/ExoJS/blob/main/src/input/GamepadAxis.ts#L100)
