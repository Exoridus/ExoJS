---
title: "Destroyable"
description: "Structural interface for anything that holds resources requiring explicit release. ExoJS value types tend to implement this even when no external resources are held (no-op `destroy()`) so consumers can call uniformly."
symbol: "Destroyable"
subsystem: "core"
importPath: "@codexo/exojs"
sourcePath: "src/core/types.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/core/types.ts#L60"
---
# Destroyable
`import { Destroyable } from '@codexo/exojs'`
Structural interface for anything that holds resources requiring explicit
release. ExoJS value types tend to implement this even when no external
resources are held (no-op `destroy()`) so consumers can call uniformly.
## Properties

- `destroy: unknown`
Source: [src/core/types.ts](https://github.com/Exoridus/ExoJS/blob/main/src/core/types.ts#L60)
