---
title: "TextureFactory"
description: "AssetFactory implementation that loads PNG, JPG, WebP, AVIF, and other browser-supported raster image formats and produces a GPU-ready Texture instance. MIME type detection is performed automatically from the buffer's magic bytes; pass an explicit `mimeType` to override. Sampler state (wrap, filter, etc.) can be configured via `samplerOptions`."
symbol: "TextureFactory"
subsystem: "resources"
importPath: "@codexo/exojs"
sourcePath: "src/resources/factories/TextureFactory.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/factories/TextureFactory.ts#L26"
---
# TextureFactory
`import { TextureFactory } from '@codexo/exojs'`
AssetFactory implementation that loads PNG, JPG, WebP, AVIF, and
other browser-supported raster image formats and produces a GPU-ready
Texture instance.

MIME type detection is performed automatically from the buffer's magic bytes;
pass an explicit `mimeType` to override. Sampler state (wrap, filter, etc.)
can be configured via `samplerOptions`.
## Constructors

- `new(): TextureFactory<>`

## Methods

- `create(source: ArrayBuffer, options: TextureFactoryOptions): Promise<Texture<>>`
- `createObjectUrl(blob: Blob): string`
- `destroy(): void`
- `process(response: Response): Promise<ArrayBuffer>`
- `revokeObjectUrl(objectUrl: string): void`

## Properties

- `_objectUrls: string[]`
- `storageName: "texture"`
Source: [src/resources/factories/TextureFactory.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/factories/TextureFactory.ts#L26)
