---
title: "WebGl2RenderBuffer"
description: "Backend-agnostic GPU buffer descriptor. Holds the buffer type (`ArrayBuffer` / `ElementArrayBuffer`), usage hint (`Static` / `Dynamic` / `Stream`), the typed-array CPU-side data, and a version counter so the runtime can detect mutations and re-upload lazily. The actual `WebGLBuffer` lifecycle is managed by the runtime binding via WebGl2RenderBufferRuntime."
symbol: "WebGl2RenderBuffer"
subsystem: "rendering"
importPath: "@codexo/exojs"
sourcePath: "src/rendering/webgl2/WebGl2RenderBuffer.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgl2/WebGl2RenderBuffer.ts#L21"
---
# WebGl2RenderBuffer
`import { WebGl2RenderBuffer } from '@codexo/exojs'`
Backend-agnostic GPU buffer descriptor. Holds the buffer type
(`ArrayBuffer` / `ElementArrayBuffer`), usage hint
(`Static` / `Dynamic` / `Stream`), the typed-array CPU-side data, and a
version counter so the runtime can detect mutations and re-upload
lazily. The actual `WebGLBuffer` lifecycle is managed by the runtime
binding via WebGl2RenderBufferRuntime.
## Constructors

- `new(type: BufferTypes, data: DataContainer, usage: BufferUsage): WebGl2RenderBuffer<>`

## Methods

- `bind(): void`
- `connect(runtime: WebGl2RenderBufferRuntime): this`
- `destroy(): void`
- `disconnect(): this`
- `upload(data: DataContainer, offset: number): void`
Source: [src/rendering/webgl2/WebGl2RenderBuffer.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgl2/WebGl2RenderBuffer.ts#L21)
