---
title: "SwitchProGamepadMapping"
description: "Mapping for the Nintendo Switch Pro Controller connected via USB or Bluetooth. Inherits the full GenericDualAnalogGamepadMapping layout. The Capture button is surfaced through the Capture channel; Home maps to Guide. Note that on some browsers the controller is only recognised after being paired through Steam or a dedicated driver."
symbol: "SwitchProGamepadMapping"
kind: "class"
subsystem: "input"
importPath: "@codexo/exojs"
memberCount: 6
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/input/SwitchProGamepadMapping.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/input/SwitchProGamepadMapping.ts"
---
## Import

`import { SwitchProGamepadMapping } from '@codexo/exojs'`

Mapping for the Nintendo Switch Pro Controller connected via USB or
Bluetooth.

Inherits the full GenericDualAnalogGamepadMapping layout. The
Capture button is surfaced through the Capture channel; Home maps to Guide.
Note that on some browsers the controller is only recognised after being
paired through Steam or a dedicated driver.

## Constructors

- `new(): SwitchProGamepadMapping`

## Methods

- `destroy(): void` - Releases all button and axis control references held by this mapping. Call when the associated gamepad disconnects to allow garbage collection.
- `hasChannel(channel: GamepadButtonChannel | GamepadAxisChannel): boolean` - Returns true when this mapping declares at least one button or axis control that writes to channel. Use to detect device-specific capabilities at runtime — e.g. before binding an input to a right-stick channel that may not exist on a single Joy-Con.

## Properties

- `axes: readonly GamepadAxis[]` - Ordered list of axes, indexed by the Gamepad API axis index.
- `buttons: readonly GamepadButton[]` - Ordered list of buttons, indexed by the Gamepad API button index.
- `family: SwitchPro` - Identifies the device family this mapping targets.

## Source

[src/input/SwitchProGamepadMapping.ts](https://github.com/Exoridus/ExoJS/blob/main/src/input/SwitchProGamepadMapping.ts)
