---
title: "WebGpuStorageBuffer"
description: "Owning wrapper around a `GPUBuffer` allocated with `STORAGE | COPY_DST | COPY_SRC`. Designed for the data-oriented ParticleSystem GPU path: one storage buffer per SoA channel, written to from CPU each spawn frame, read by compute shaders, and (optionally) read back to CPU for expire detection. Lifetime: the caller (typically `ParticleGpuState`) owns the buffer and must call destroy when the system is torn down."
symbol: "WebGpuStorageBuffer"
kind: "class"
subsystem: "rendering"
importPath: "@codexo/exojs"
memberCount: 8
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/rendering/webgpu/compute/WebGpuStorageBuffer.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgpu/compute/WebGpuStorageBuffer.ts#L12"
---
## Import

`import { WebGpuStorageBuffer } from '@codexo/exojs'`

Owning wrapper around a `GPUBuffer` allocated with `STORAGE | COPY_DST | COPY_SRC`.
Designed for the data-oriented ParticleSystem GPU path: one storage
buffer per SoA channel, written to from CPU each spawn frame, read by
compute shaders, and (optionally) read back to CPU for expire detection.

Lifetime: the caller (typically `ParticleGpuState`) owns the buffer and
must call destroy when the system is torn down.

## Constructors

- `new(device: GPUDevice, byteLength: number, label: string): WebGpuStorageBuffer`

## Methods

- `destroy(): void`
- `read(target: ArrayBufferView, encoder?: GPUCommandEncoder): Promise<void>`
- `write(data: ArrayBufferView, byteOffset: number, byteSize?: number): void`

## Properties

- `buffer: GPUBuffer`
- `byteLength: number`
- `device: GPUDevice`
- `label: string`

## Source

[src/rendering/webgpu/compute/WebGpuStorageBuffer.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgpu/compute/WebGpuStorageBuffer.ts#L12)
