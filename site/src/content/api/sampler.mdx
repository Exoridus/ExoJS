---
title: "Sampler"
description: "WebGL2 sampler object wrapper that controls texture filtering and wrapping. Creates a `WebGLSampler` on construction and updates its parameters whenever `scaleMode` or `wrapMode` changes. Bind the sampler to a texture unit with bind before issuing draw calls. Note: `premultiplyAlpha`, `generateMipMap`, and `flipY` are pixel-store parameters handled at texture upload time; this class only manages filter and wrap state."
symbol: "Sampler"
kind: "class"
subsystem: "rendering"
importPath: "@codexo/exojs"
memberCount: 8
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/rendering/texture/Sampler.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/texture/Sampler.ts"
---
## Import

`import { Sampler } from '@codexo/exojs'`

WebGL2 sampler object wrapper that controls texture filtering and wrapping.

Creates a `WebGLSampler` on construction and updates its parameters whenever
`scaleMode` or `wrapMode` changes. Bind the sampler to a texture unit with
bind before issuing draw calls. Note: `premultiplyAlpha`, `generateMipMap`,
and `flipY` are pixel-store parameters handled at texture upload time; this class
only manages filter and wrap state.

## Constructors

- `new(gl: WebGL2RenderingContext, options: SamplerOptions): Sampler`

## Methods

- `bind(textureUnit: number): this` - Bind this sampler to textureUnit for the next draw call. Must be called each time the active texture unit changes.
- `destroy(): void`
- `setScaleMode(scaleMode: ScaleModes): this`
- `setWrapMode(wrapMode: WrapModes): this`

## Properties

- `sampler: WebGLSampler | null`
- `scaleMode: ScaleModes`
- `wrapMode: WrapModes`

## Source

[src/rendering/texture/Sampler.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/texture/Sampler.ts)
