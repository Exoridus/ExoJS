---
title: "FactoryRegistry"
description: "Maps AssetConstructor tokens to their corresponding AssetFactory instances. Resolution is prototype-chain aware: if no factory is registered for the exact constructor, the registry walks up the prototype chain and returns the first ancestor match. This means registering a factory for a base class automatically handles all subclasses. Used by Loader; consumers interact through `loader.register()`."
symbol: "FactoryRegistry"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 5
sections: ["Import", "Constructors", "Methods", "Source"]
sourcePath: "src/resources/FactoryRegistry.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/FactoryRegistry.ts#L21"
---
## Import

`import { FactoryRegistry } from '@codexo/exojs'`

Maps AssetConstructor tokens to their corresponding
AssetFactory instances.

Resolution is prototype-chain aware: if no factory is registered for the
exact constructor, the registry walks up the prototype chain and returns the
first ancestor match. This means registering a factory for a base class
automatically handles all subclasses.

 Used by Loader; consumers interact through `loader.register()`.

## Constructors

- `new(): FactoryRegistry`

## Methods

- `destroy(): void`
- `has(type: AssetConstructor): boolean`
- `register(type: AssetConstructor<T>, factory: AssetFactory<T>): void`
- `resolve(type: AssetConstructor<T>): AssetFactory<T>`

## Source

[src/resources/FactoryRegistry.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/FactoryRegistry.ts#L21)
