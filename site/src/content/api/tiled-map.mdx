---
title: "TiledMap"
description: "A parsed and validated Tiled map (`.tmj`). `TiledMap` represents the parsed Tiled source format. `TileMap` is the format-independent ExoJS runtime map used for rendering, queries, and mutation. Construction validates that tilesets cover a non-overlapping, duplicate-free range of global tile ids, and that every GID referenced by layers (tile layer cells, infinite-map chunks, and tile object `gid`s) falls within one of those ranges. Both checks throw TiledFormatError on failure."
symbol: "TiledMap"
kind: "class"
subsystem: "tiled"
importPath: "@codexo/exojs-tiled"
memberCount: 19
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "packages/exojs-tiled/src/TiledMap.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-tiled/src/TiledMap.ts"
---
## Import

`import { TiledMap } from '@codexo/exojs-tiled'`

A parsed and validated Tiled map (`.tmj`).

`TiledMap` represents the parsed Tiled source format. `TileMap` is the
format-independent ExoJS runtime map used for rendering, queries, and
mutation.

Construction validates that tilesets cover a non-overlapping,
duplicate-free range of global tile ids, and that every GID referenced by
layers (tile layer cells, infinite-map chunks, and tile object
`gid`s) falls within one of those ranges. Both checks throw
TiledFormatError on failure.

## Constructors

- `new(source: string, data: TiledMapData, tilesets: readonly TiledTileset[], imageTextures: ReadonlyMap<number, Texture>): TiledMap`

## Methods

- `destroy(): void` - Releases this map's reference to its parsed source. Tileset textures are Loader-owned and may be shared with other maps; this does NOT destroy them.
- `findTilesetForGid(gid: number): TiledTileset | undefined` - Returns the tileset that owns gid, or undefined if gid is 0 (the empty-cell sentinel) or is not covered by any tileset. Flip/rotation flag bits are masked off before the range lookup, so the raw GID values found in TiledTileLayer.data/chunks and TiledObject.gid can be passed directly.
- `getProperty(name: string): TiledPropertyData | undefined` - Looks up a custom property by name.
- `toTileMap(): TileMap` - Convert this parsed Tiled source model into a format-independent runtime TileMap from @codexo/exojs-tilemap. Only finite orthogonal maps with atlas tilesets are supported. A non-orthogonal or infinite map, or a collection-of-images tileset, throws TiledFormatError rather than silently producing wrong (misplaced) or empty geometry. Tile layers become renderable TileLayers, object groups become data-only ObjectLayers, and image layers become data-only ImageLayers; group layer children are flattened in document order. The returned TileMap does **not** own the tileset textures — they remain in the Loader cache. Destroying the returned map does not unload textures.

## Properties

- `backgroundColor: string`
- `class: string`
- `data: TiledMapData` - The validated raw map data this instance was built from.
- `height: number` - Map height in tiles.
- `infinite: boolean`
- `layers: readonly TiledLayer[]`
- `orientation: TiledOrientation`
- `properties: readonly TiledPropertyData[]`
- `renderOrder: TiledRenderOrder`
- `source: string` - Resolved URL this map was loaded from.
- `tileHeight: number` - Tile grid cell height in pixels.
- `tilesets: readonly TiledTileset[]` - Tilesets used by this map, sorted by TiledTileset.firstGid ascending.
- `tileWidth: number` - Tile grid cell width in pixels.
- `width: number` - Map width in tiles.

## Source

[packages/exojs-tiled/src/TiledMap.ts](https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-tiled/src/TiledMap.ts)
