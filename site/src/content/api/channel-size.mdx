---
title: "ChannelSize"
description: "Slot-count constants for the unified channels buffer. The buffer is split into three categories (`Keyboard`, `Pointers`, `Gamepads`) of 256 slots each — `Category`. `Gamepad` is the per-device sub-allocation inside the `Gamepads` category (4 devices × 64 channels each)."
symbol: "ChannelSize"
kind: "enum"
subsystem: "input"
importPath: "@codexo/exojs"
memberCount: 3
tier: "stable"
sections: ["Import", "Members", "Source"]
sourcePath: "src/input/types.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/input/types.ts#L7"
---
## Import

`import { ChannelSize } from '@codexo/exojs'`

Slot-count constants for the unified channels buffer. The buffer is split
into three categories (`Keyboard`, `Pointers`, `Gamepads`) of 256 slots
each — `Category`. `Gamepad` is the per-device sub-allocation inside the
`Gamepads` category (4 devices × 64 channels each).

## Members

- `Category`
- `Container`
- `Gamepad`

## Source

[src/input/types.ts](https://github.com/Exoridus/ExoJS/blob/main/src/input/types.ts#L7)
