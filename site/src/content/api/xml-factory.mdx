---
title: "XmlFactory"
description: "AssetFactory implementation that loads XML files and produces a parsed Document via the browser's built-in DOMParser. Throws if the response body is not well-formed XML. Use the returned `Document` directly with standard DOM APIs — `doc.querySelector`, `doc.getElementsByTagName`, etc. — or pass it to a format-specific parser (e.g. for Tiled TMX, BMFont, or TexturePacker XML atlas files)."
symbol: "XmlFactory"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 7
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/resources/factories/XmlFactory.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/factories/XmlFactory.ts#L12"
---
## Import

`import { XmlFactory } from '@codexo/exojs'`

AssetFactory implementation that loads XML files and produces a
parsed Document via the browser's built-in DOMParser.

Throws if the response body is not well-formed XML. Use the returned
`Document` directly with standard DOM APIs — `doc.querySelector`,
`doc.getElementsByTagName`, etc. — or pass it to a format-specific parser
(e.g. for Tiled TMX, BMFont, or TexturePacker XML atlas files).

## Constructors

- `new(): XmlFactory`

## Methods

- `create(source: string): Promise<Document>`
- `createObjectUrl(blob: Blob): string`
- `destroy(): void`
- `process(response: Response): Promise<string>`
- `revokeObjectUrl(objectUrl: string): void`

## Properties

- `storageName: "xml"`

## Source

[src/resources/factories/XmlFactory.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/factories/XmlFactory.ts#L12)
