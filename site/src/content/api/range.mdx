---
title: "Range"
description: "Uniform random number in `[min, max]`. Each `sample()` returns a fresh roll; the bounds are inclusive on both ends (modulo the rounding bias inherent to `Math.random()`)."
symbol: "Range"
kind: "class"
subsystem: "particles"
importPath: "@codexo/exojs"
memberCount: 4
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/particles/distributions/Range.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/particles/distributions/Range.ts#L8"
---
## Import

`import { Range } from '@codexo/exojs'`

Uniform random number in `[min, max]`. Each `sample()` returns a fresh
roll; the bounds are inclusive on both ends (modulo the rounding bias
inherent to `Math.random()`).

## Constructors

- `new(min: number, max: number): Range`

## Methods

- `sample(): number`

## Properties

- `max: number`
- `min: number`

## Source

[src/particles/distributions/Range.ts](https://github.com/Exoridus/ExoJS/blob/main/src/particles/distributions/Range.ts#L8)
