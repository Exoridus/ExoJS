---
title: "Range"
description: "Uniform random number in `[min, max]`. Each `sample()` returns a fresh roll; the bounds are inclusive on both ends (modulo the rounding bias inherent to `Math.random()`)."
symbol: "Range"
kind: "class"
subsystem: "particles"
importPath: "@codexo/exojs-particles"
memberCount: 4
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "packages/exojs-particles/src/distributions/Range.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/distributions/Range.ts#L8"
---
## Import

`import { Range } from '@codexo/exojs-particles'`

Uniform random number in `[min, max]`. Each `sample()` returns a fresh
roll; the bounds are inclusive on both ends (modulo the rounding bias
inherent to `Math.random()`).

## Constructors

- `new(min: number, max: number): Range`

## Methods

- `sample(): number`

## Properties

- `max: number`
- `min: number`

## Source

[packages/exojs-particles/src/distributions/Range.ts](https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-particles/src/distributions/Range.ts#L8)
