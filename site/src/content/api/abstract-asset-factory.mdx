---
title: "AbstractAssetFactory"
description: "Shared base class for all built-in AssetFactory implementations. Manages a pool of object URLs created during asset loading so that they can be revoked in bulk when the factory is destroyed, preventing memory leaks. Concrete subclasses must implement process and create; they may call createObjectUrl instead of `URL.createObjectURL` directly so that the URL is automatically tracked and cleaned up."
symbol: "AbstractAssetFactory"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 7
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/resources/AbstractAssetFactory.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/AbstractAssetFactory.ts"
---
## Import

`import { AbstractAssetFactory } from '@codexo/exojs'`

Shared base class for all built-in AssetFactory implementations.

Manages a pool of object URLs created during asset loading so that they can
be revoked in bulk when the factory is destroyed, preventing memory leaks.
Concrete subclasses must implement process and create; they
may call createObjectUrl instead of `URL.createObjectURL` directly
so that the URL is automatically tracked and cleaned up.

## Constructors

- `new(): AbstractAssetFactory<T>`

## Methods

- `create(source: unknown, options?: unknown): Promise<T>` - Constructs the final engine asset from the intermediate value produced by process (or retrieved from cache).
- `createObjectUrl(blob: Blob): string` - Creates an object URL from blob and registers it for automatic cleanup when destroy is called. Prefer this over calling URL.createObjectURL directly inside a factory subclass so the URL is always tracked.
- `destroy(): void` - Revokes every object URL that was created via createObjectUrl and clears the internal tracking pool.
- `process(response: Response): Promise<unknown>` - Converts a raw HTTP response into a serialisable intermediate value suitable for cache storage and later passed to create.
- `revokeObjectUrl(objectUrl: string): void`

## Properties

- `storageName: string` - Identifier used as the object-store / storage-namespace key when this factory's assets are persisted to a CacheStore.

## Source

[src/resources/AbstractAssetFactory.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/AbstractAssetFactory.ts)
