---
title: "GenericDualAnalogGamepadMapping"
description: "Baseline mapping for dual-analog controllers that follow the standard W3C Gamepad API layout (axes 0–3 for both sticks, axes 4–7 auxiliary). Each signed stick axis is exposed three ways for ergonomic binding: - Two direction-split, non-negative channels (e.g. `LeftStickLeft` / `LeftStickRight`) for \"buttons-style\" subscriptions. - One signed aggregate channel (e.g. `LeftStickX`) for direct -1..1 consumption — useful for movement or aiming. Device-specific subclasses (Xbox, PlayStation, Switch Pro, etc.) inherit this layout and override only GamepadMapping.family."
symbol: "GenericDualAnalogGamepadMapping"
kind: "class"
subsystem: "input"
importPath: "@codexo/exojs"
memberCount: 6
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/input/GenericDualAnalogGamepadMapping.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/input/GenericDualAnalogGamepadMapping.ts"
---
## Import

`import { GenericDualAnalogGamepadMapping } from '@codexo/exojs'`

Baseline mapping for dual-analog controllers that follow the standard
W3C Gamepad API layout (axes 0–3 for both sticks, axes 4–7 auxiliary).

Each signed stick axis is exposed three ways for ergonomic binding:
 - Two direction-split, non-negative channels (e.g. `LeftStickLeft` /
   `LeftStickRight`) for "buttons-style" subscriptions.
 - One signed aggregate channel (e.g. `LeftStickX`) for direct -1..1
   consumption — useful for movement or aiming.

Device-specific subclasses (Xbox, PlayStation, Switch Pro, etc.) inherit
this layout and override only GamepadMapping.family.

## Constructors

- `new(): GenericDualAnalogGamepadMapping`

## Methods

- `destroy(): void` - Releases all button and axis control references held by this mapping. Call when the associated gamepad disconnects to allow garbage collection.
- `hasChannel(channel: GamepadButtonChannel | GamepadAxisChannel): boolean` - Returns true when this mapping declares at least one button or axis control that writes to channel. Use to detect device-specific capabilities at runtime — e.g. before binding an input to a right-stick channel that may not exist on a single Joy-Con.

## Properties

- `axes: readonly GamepadAxis[]` - Ordered list of axes, indexed by the Gamepad API axis index.
- `buttons: readonly GamepadButton[]` - Ordered list of buttons, indexed by the Gamepad API button index.
- `family: GamepadMappingFamily` - Identifies the device family this mapping targets.

## Source

[src/input/GenericDualAnalogGamepadMapping.ts](https://github.com/Exoridus/ExoJS/blob/main/src/input/GenericDualAnalogGamepadMapping.ts)
