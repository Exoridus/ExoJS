---
title: "CsvFactory"
description: "AssetFactory implementation that loads CSV (and TSV) files and produces a two-dimensional array of strings. Parsing is RFC 4180-compliant: quoted fields (including fields that contain the delimiter, newlines, or double-quotes) are handled correctly. Line endings are normalised before parsing; empty trailing lines are dropped. The returned `string[][]` is intentionally untyped — callers decide whether to treat the first row as headers, apply type coercions, etc."
symbol: "CsvFactory"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 7
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/resources/factories/CsvFactory.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/factories/CsvFactory.ts#L23"
---
## Import

`import { CsvFactory } from '@codexo/exojs'`

AssetFactory implementation that loads CSV (and TSV) files and
produces a two-dimensional array of strings.

Parsing is RFC 4180-compliant: quoted fields (including fields that contain
the delimiter, newlines, or double-quotes) are handled correctly. Line
endings are normalised before parsing; empty trailing lines are dropped.

The returned `string[][]` is intentionally untyped — callers decide whether
to treat the first row as headers, apply type coercions, etc.

## Constructors

- `new(): CsvFactory`

## Methods

- `create(source: string, options: CsvFactoryOptions): Promise<string[][]>`
- `createObjectUrl(blob: Blob): string`
- `destroy(): void`
- `process(response: Response): Promise<string>`
- `revokeObjectUrl(objectUrl: string): void`

## Properties

- `storageName: "csv"`

## Source

[src/resources/factories/CsvFactory.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/factories/CsvFactory.ts#L23)
