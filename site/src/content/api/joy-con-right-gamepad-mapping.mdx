---
title: "JoyConRightGamepadMapping"
description: "Mapping for the Nintendo Joy-Con (R) held horizontally as a solo controller. Declares only channels that physically exist on the device — one stick (mapped to the LeftStick channels to match the W3C standard layout for the lone reported stick regardless of which Joy-Con reports it), four face buttons, the SL/SR inner shoulders (routed through the standard shoulder channels), Plus, the Home button, and the stick-click. Right-stick channels, triggers, Minus/Capture, Touchpad, paddles, and auxiliary axes are intentionally absent. Use GamepadMapping.hasChannel to detect availability before binding inputs that may not exist on every device family."
symbol: "JoyConRightGamepadMapping"
kind: "class"
subsystem: "input"
importPath: "@codexo/exojs"
memberCount: 6
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/input/JoyConRightGamepadMapping.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/input/JoyConRightGamepadMapping.ts#L19"
---
## Import

`import { JoyConRightGamepadMapping } from '@codexo/exojs'`

Mapping for the Nintendo Joy-Con (R) held horizontally as a solo controller.

Declares only channels that physically exist on the device — one stick
(mapped to the LeftStick channels to match the W3C standard layout for the
lone reported stick regardless of which Joy-Con reports it), four face
buttons, the SL/SR inner shoulders (routed through the standard shoulder
channels), Plus, the Home button, and the stick-click.

Right-stick channels, triggers, Minus/Capture, Touchpad, paddles, and
auxiliary axes are intentionally absent. Use
GamepadMapping.hasChannel to detect availability before binding
inputs that may not exist on every device family.

## Constructors

- `new(): JoyConRightGamepadMapping`

## Methods

- `destroy(): void`
- `hasChannel(channel: GamepadButtonChannel | GamepadAxisChannel): boolean`

## Properties

- `axes: readonly GamepadAxis[]`
- `buttons: readonly GamepadButton[]`
- `family: JoyConRight`

## Source

[src/input/JoyConRightGamepadMapping.ts](https://github.com/Exoridus/ExoJS/blob/main/src/input/JoyConRightGamepadMapping.ts#L19)
