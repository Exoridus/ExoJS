---
title: "Material"
description: "Describes the look of a renderable — shader, uniforms, textures, blend mode, and sampling state — independent of its geometry. A `Material` can be shared across many drawables; renderers cache compiled programs/pipelines keyed on pipelineKey and reuse bindings keyed on bindKey. Subclasses fix the target drawable class. Call destroy when the material is no longer needed to release the GPU resources cached on every backend it was used on. Both keys are derived live from the current material state, so they stay stable across repeated reads and change exactly when the relevant state changes — even when uniforms, textures, blendMode, or sampler are mutated in place."
symbol: "Material"
kind: "class"
subsystem: "rendering"
importPath: "@codexo/exojs"
memberCount: 12
tier: "advanced"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/rendering/material/Material.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/material/Material.ts"
---
## Import

`import { Material } from '@codexo/exojs'`

Describes the look of a renderable — shader, uniforms, textures, blend
mode, and sampling state — independent of its geometry.

A `Material` can be shared across many drawables; renderers cache
compiled programs/pipelines keyed on pipelineKey and reuse
bindings keyed on bindKey. Subclasses fix the target
drawable class. Call destroy when the material is no longer
needed to release the GPU resources cached on every backend it was used
on.

Both keys are derived live from the current material state, so they stay
stable across repeated reads and change exactly when the relevant state
changes — even when uniforms, textures, blendMode,
or sampler are mutated in place.

## Constructors

- `new(options: MaterialOptions): Material`

## Methods

- `destroy(): void` - Release GPU resources cached against this material on every backend that has compiled it. Safe to call multiple times. After destroy, the material can still be re-used — renderers recompile on next draw — but typical usage is to drop the reference.
- `setTexture(name: string, texture: Texture | RenderTexture): this` - Bind a named texture. Equivalent to material.textures[name] = texture, returned this for chaining.
- `setUniform(name: string, value: UniformValue): this` - Set a uniform value. Equivalent to material.uniforms[name] = value, returned this for chaining.

## Properties

- `blendMode: BlendModes` - Compositing blend mode applied when drawing with this material.
- `sampler: SamplerOptions | null` - Backend-agnostic sampling state descriptor, or null for the default.
- `shader: ShaderSource` - GLSL/WGSL source pair backing this material.
- `target: "mesh" | "sprite"` - Which drawable class this material can serve; renderers check compatibility.
- `textures: Record<string, Texture | RenderTexture>` - Named texture bindings claimed in addition to the drawable's own texture.
- `uniforms: Record<string, UniformValue>` - Mutable user uniform values. Mutate between frames to drive animated effects; the renderer reads from this map every draw. material.uniforms.u_time = performance.now() / 1000; material.uniforms.u_color = [1, 0.5, 0, 1];
- `bindKey: number` - Stable bind key: identical ⇒ same bindings (textures unchanged). Derived from this material's identity and the identities of its bound textures. Changes when a texture is swapped; drives bind-group/slot reuse.
- `pipelineKey: number` - Stable pipeline key: identical ⇒ same GPU pipeline/program can be used. Derived from shader identity, blend mode, and sampler state, and is independent of the owning material instance so identically configured materials share a pipeline. Drives grouping and the pipeline cache.

## Source

[src/rendering/material/Material.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/material/Material.ts)
