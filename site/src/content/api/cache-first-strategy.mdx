---
title: "CacheFirstStrategy"
description: "CacheStrategy that checks every provided CacheStore before falling back to the network. On a cache hit the stored value is fed directly to factory.create; if that throws (stale or corrupt entry) the entry is deleted and the next store is tried. Only once all stores miss does the strategy fetch from the network and write the processed source back to every store. Quota or serialisation errors during write are swallowed silently so that a full storage can never prevent an asset from loading. Returns the fully constructed resource — callers do not need to call AssetFactory.create again."
symbol: "CacheFirstStrategy"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 2
sections: ["Import", "Constructors", "Methods", "Source"]
sourcePath: "src/resources/CacheFirstStrategy.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/CacheFirstStrategy.ts#L19"
---
## Import

`import { CacheFirstStrategy } from '@codexo/exojs'`

CacheStrategy that checks every provided CacheStore before
falling back to the network.

On a cache hit the stored value is fed directly to
factory.create; if that throws (stale or
corrupt entry) the entry is deleted and the next store is tried. Only once
all stores miss does the strategy fetch from the network and write the
processed source back to every store. Quota or serialisation errors during
write are swallowed silently so that a full storage can never prevent an
asset from loading.

Returns the fully constructed resource — callers do not need to call
AssetFactory.create again.

## Constructors

- `new(): CacheFirstStrategy`

## Methods

- `resolve(request: CacheRequest, stores: readonly CacheStore[]): Promise<unknown>`

## Source

[src/resources/CacheFirstStrategy.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/CacheFirstStrategy.ts#L19)
