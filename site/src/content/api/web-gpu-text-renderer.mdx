---
title: "WebGpuTextRenderer"
description: "WebGPU renderer for Text and BitmapText nodes. Mirrors WebGl2TextRenderer: per-node style data is packed once per flush into a `var<storage, read>` buffer, three specialised WGSL fragment variants handle SDF / MSDF / colour-atlas glyphs, and quads are sorted and batched by (shaderType, atlasPage) to minimise draw calls within a single render pass."
symbol: "WebGpuTextRenderer"
kind: "class"
subsystem: "rendering"
importPath: "@codexo/exojs"
memberCount: 12
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/rendering/webgpu/WebGpuTextRenderer.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgpu/WebGpuTextRenderer.ts#L241"
---
## Import

`import { WebGpuTextRenderer } from '@codexo/exojs'`

WebGPU renderer for Text and BitmapText nodes.

Mirrors WebGl2TextRenderer: per-node style data is packed once per
flush into a `var\<storage, read>` buffer, three specialised WGSL fragment
variants handle SDF / MSDF / colour-atlas glyphs, and quads are sorted and
batched by (shaderType, atlasPage) to minimise draw calls within a single
render pass.

## Constructors

- `new(): WebGpuTextRenderer`

## Methods

- `connect(backend: WebGpuBackend): void`
- `destroy(): void`
- `disconnect(): void`
- `flush(): void`
- `getBackend(): WebGpuBackend`
- `getBackendOrNull(): WebGpuBackend | null`
- `onConnect(backend: WebGpuBackend): void`
- `onDisconnect(): void`
- `prewarmPipelines(formats: readonly GPUTextureFormat[]): Promise<void>`
- `render(node: BitmapText | Text): void`

## Properties

- `backendType: WebGpu`

## Source

[src/rendering/webgpu/WebGpuTextRenderer.ts](https://github.com/Exoridus/ExoJS/blob/main/src/rendering/webgpu/WebGpuTextRenderer.ts#L241)
