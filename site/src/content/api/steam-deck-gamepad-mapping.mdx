---
title: "SteamDeckGamepadMapping"
description: "Mapping for the Valve Steam Deck (and the new Valve Controller via vendor fallback) when its raw HID gamepad is exposed directly to the browser — i.e. when Steam Input is *not* intercepting the device. With Steam Input intercepting, the device appears as `28de:11ff` \"Steam Virtual Gamepad\" with a standard W3C layout instead, and is routed to GenericDualAnalogGamepadMapping. The raw layout is non-standard: face buttons live at indices 3-6 (not the W3C-standard 0-3), the D-pad lives at indices 16-19, paddles at 20-23, and triggers report as analog axes 8/9 rather than buttons 6/7. Indices are derived from the Linux SDL_GameControllerDB entry for `Valve Steam Deck`."
symbol: "SteamDeckGamepadMapping"
kind: "class"
subsystem: "input"
importPath: "@codexo/exojs"
memberCount: 6
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/input/SteamDeckGamepadMapping.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/input/SteamDeckGamepadMapping.ts"
---
## Import

`import { SteamDeckGamepadMapping } from '@codexo/exojs'`

Mapping for the Valve Steam Deck (and the new Valve Controller via vendor
fallback) when its raw HID gamepad is exposed directly to the browser —
i.e. when Steam Input is *not* intercepting the device. With Steam Input
intercepting, the device appears as `28de:11ff` "Steam Virtual Gamepad"
with a standard W3C layout instead, and is routed to
GenericDualAnalogGamepadMapping.

The raw layout is non-standard: face buttons live at indices 3-6 (not the
W3C-standard 0-3), the D-pad lives at indices 16-19, paddles at 20-23, and
triggers report as analog axes 8/9 rather than buttons 6/7. Indices are
derived from the Linux SDL_GameControllerDB entry for `Valve Steam Deck`.

## Constructors

- `new(): SteamDeckGamepadMapping`

## Methods

- `destroy(): void` - Releases all button and axis control references held by this mapping. Call when the associated gamepad disconnects to allow garbage collection.
- `hasChannel(channel: GamepadButtonChannel | GamepadAxisChannel): boolean` - Returns true when this mapping declares at least one button or axis control that writes to channel. Use to detect device-specific capabilities at runtime — e.g. before binding an input to a right-stick channel that may not exist on a single Joy-Con.

## Properties

- `axes: readonly GamepadAxis[]` - Ordered list of axes, indexed by the Gamepad API axis index.
- `buttons: readonly GamepadButton[]` - Ordered list of buttons, indexed by the Gamepad API button index.
- `family: GamepadMappingFamily` - Identifies the device family this mapping targets.

## Source

[src/input/SteamDeckGamepadMapping.ts](https://github.com/Exoridus/ExoJS/blob/main/src/input/SteamDeckGamepadMapping.ts)
