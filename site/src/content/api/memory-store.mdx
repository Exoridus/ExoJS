---
title: "MemoryStore"
description: "In-memory KeyValueStore backed by a `Map`. Holds values **by reference** — no cloning, no serialization — so it accepts any value but does not persist across sessions and does not isolate the caller from later mutation of a stored object. Ideal for tests and ephemeral data, and as a drop-in store when persistence is not needed."
symbol: "MemoryStore"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 6
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Source"]
sourcePath: "src/resources/MemoryStore.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/MemoryStore.ts"
---
## Import

`import { MemoryStore } from '@codexo/exojs'`

In-memory KeyValueStore backed by a `Map`. Holds values **by
reference** — no cloning, no serialization — so it accepts any value but does
not persist across sessions and does not isolate the caller from later
mutation of a stored object. Ideal for tests and ephemeral data, and as a
drop-in store when persistence is not needed.

## Constructors

- `new(): MemoryStore`

## Methods

- `clear(): Promise<boolean>`
- `delete(key: string): Promise<boolean>`
- `get(key: string): Promise<T | null>`
- `has(key: string): Promise<boolean>`
- `set(key: string, value: unknown): Promise<void>`

## Source

[src/resources/MemoryStore.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/MemoryStore.ts)
