---
title: "TiledFormatError"
description: "Thrown when a Tiled JSON document does not match the expected shape. `source` is the URL of the file being parsed; `path` is a JSON-path-like pointer (e.g. `layers[2].objects[0].properties[1].value`) to the offending value, or `''` for the document root."
symbol: "TiledFormatError"
kind: "class"
subsystem: "tiled"
importPath: "@codexo/exojs-tiled"
memberCount: 7
tier: "stable"
sections: ["Import", "Constructors", "Properties", "Source"]
sourcePath: "packages/exojs-tiled/src/validate.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-tiled/src/validate.ts#L41"
---
## Import

`import { TiledFormatError } from '@codexo/exojs-tiled'`

Thrown when a Tiled JSON document does not match the expected shape.
`source` is the URL of the file being parsed; `path` is a JSON-path-like
pointer (e.g. `layers[2].objects[0].properties[1].value`) to the offending
value, or `''` for the document root.

## Constructors

- `new(source: string, path: string, message: string): TiledFormatError`

## Properties

- `cause: unknown`
- `message: string`
- `name: string`
- `path: string`
- `source: string`
- `stack: string`

## Source

[packages/exojs-tiled/src/validate.ts](https://github.com/Exoridus/ExoJS/blob/main/packages/exojs-tiled/src/validate.ts#L41)
