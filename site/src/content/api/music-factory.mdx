---
title: "MusicFactory"
description: "AssetFactory implementation that loads streaming audio assets (MP3, OGG, WAV, AAC, and other browser-supported formats) and produces an AudioStream instance backed by an `<audio>` element. Unlike SoundFactory, stream assets are decoded lazily via the browser's streaming audio pipeline rather than being fully decoded into an AudioBuffer up-front, making them appropriate for long-form background tracks. The underlying `<audio>` elements are paused and detached when MusicFactory.destroy is called."
symbol: "MusicFactory"
kind: "class"
subsystem: "resources"
importPath: "@codexo/exojs"
memberCount: 7
tier: "stable"
sections: ["Import", "Constructors", "Methods", "Properties", "Source"]
sourcePath: "src/resources/factories/MusicFactory.ts"
sourceUrl: "https://github.com/Exoridus/ExoJS/blob/main/src/resources/factories/MusicFactory.ts"
---
## Import

`import { MusicFactory } from '@codexo/exojs'`

AssetFactory implementation that loads streaming audio assets
(MP3, OGG, WAV, AAC, and other browser-supported formats) and produces an
AudioStream instance backed by an `\<audio>` element.

Unlike SoundFactory, stream assets are decoded lazily via the browser's
streaming audio pipeline rather than being fully decoded into an
AudioBuffer up-front, making them appropriate for long-form
background tracks. The underlying `\<audio>` elements are paused and detached
when MusicFactory.destroy is called.

## Constructors

- `new(): MusicFactory`

## Methods

- `create(source: ArrayBuffer, options: MusicFactoryOptions): Promise<AudioStream>` - Wraps audio bytes in an \<audio> element and resolves with an AudioStream instance once the configured loadEvent fires. Rejects if the element emits an error or abort event before the load event is received.
- `createObjectUrl(blob: Blob): string` - Creates an object URL from blob and registers it for automatic cleanup when destroy is called. Prefer this over calling URL.createObjectURL directly inside a factory subclass so the URL is always tracked.
- `destroy(): void` - Pauses and resets all \<audio> elements created by this factory to release media resources, then delegates to the base AbstractAssetFactory.destroy to revoke any object URLs.
- `process(response: Response): Promise<ArrayBuffer>` - Reads the full response body as an ArrayBuffer for blob construction.
- `revokeObjectUrl(objectUrl: string): void`

## Properties

- `storageName: "music"` - Identifier used as the object-store / storage-namespace key when this factory's assets are persisted to a CacheStore.

## Source

[src/resources/factories/MusicFactory.ts](https://github.com/Exoridus/ExoJS/blob/main/src/resources/factories/MusicFactory.ts)
