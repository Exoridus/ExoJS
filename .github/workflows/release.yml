name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to publish (must already exist, e.g. v0.6.2)'
        required: true

permissions:
  contents: read
  # Required so npm-cli can mint an OIDC-token from GitHub at publish
  # time. The token both (a) authenticates against npm via the package's
  # configured Trusted Publisher (no static NPM_TOKEN secret), and
  # (b) signs the provenance attestation that lands on the npm version
  # page.
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_VERSION: '22'

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          registry-url: 'https://registry.npmjs.org'
          cache: npm

      # Trusted Publisher auth requires npm CLI 11.5+. Node 22 LTS still
      # ships with npm 10, so we upgrade explicitly. Pinned to ^11 so a
      # future npm 12 with another auth refactor doesn't surprise us.
      - name: Upgrade npm to 11+
        run: npm install -g npm@^11

      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --fund=false

      # Refuse to publish unless the workflow is running on a tag whose
      # name matches the version inside package.json. This catches the
      # "tag and package.json version drifted" mistake before it ships.
      - name: Verify tag matches package.json version
        run: |
          TAG_NAME="${GITHUB_REF#refs/tags/}"
          PKG_VERSION="v$(node -p "require('./package.json').version")"
          if [ "$TAG_NAME" != "$PKG_VERSION" ]; then
            echo "::error::Tag '$TAG_NAME' does not match package.json version '$PKG_VERSION'."
            exit 1
          fi
          echo "Tag and package.json agree on $PKG_VERSION."

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      # `prepack` rebuilds dist/ via rollup; we run it explicitly here so
      # any build failure surfaces before the publish attempt rather than
      # mid-publish.
      - name: Build
        run: npm run build

      - name: Verify package shape
        run: |
          npm run verify:exports
          npm pack --dry-run --ignore-scripts

      - name: Publish to npm
        run: npm publish --provenance --access public
