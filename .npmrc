# pnpm-specific settings — used when running with pnpm (pilot branch).
# npm reads this file too but ignores unknown keys gracefully.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
