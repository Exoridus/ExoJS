export const

    /**
     * @public
     * @static
     * @readonly
     * @constant
     * @type {Number}
     */
    TAU = Math.PI * 2,

    /**
     * @public
     * @static
     * @readonly
     * @constant
     * @type {Number}
     */
    RAD_PER_DEG = Math.PI / 180,

    /**
     * @public
     * @static
     * @readonly
     * @constant
     * @type {Number}
     */
    DEG_PER_RAD = 180 / Math.PI,

    /**
     * @public
     * @constant
     * @type {Object}
     */
    VORONOI_REGION = {
        LEFT: -1,
        MIDDLE: 0,
        RIGHT: 1,
    };
