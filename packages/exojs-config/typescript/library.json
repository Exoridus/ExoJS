{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Profile for a buildable library package (Core). Adds declaration + source-map emit defaults. Package-local config sets rootDir/outDir/declarationDir, customConditions, include/exclude.",
  "extends": "./base.json",
  "compilerOptions": {
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
